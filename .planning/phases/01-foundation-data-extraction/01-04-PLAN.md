---
phase: 01-foundation-data-extraction
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - _data/blog-inventory.yml
  - docs/ASTRO-DATA-MAPPING.md
  - scripts/validate-data.rb
  - lib/tasks/data.rake
autonomous: true
requirements:
  - DATA-04
user_setup: []
must_haves:
  truths:
    - "Every legacy blog post (51 markdown files) has a documented curation tier in _data/blog-inventory.yml"
    - "All 13 hide:true posts are tier archived"
    - "10-15 posts are tier featured after curation"
    - "2015/2024 design-doc duplicate pairs have canonical_slug pointing to 2024 entry per D-17"
    - "Phase 2 executor has ASTRO-DATA-MAPPING.md documenting Jekyll to Astro path and loader mapping per D-10"
  artifacts:
    - path: _data/blog-inventory.yml
      provides: "Complete blog curation inventory per DATA-04 and D-13"
      contains: "tier:"
    - path: docs/ASTRO-DATA-MAPPING.md
      provides: "Phase 2 handoff for _data/ to src/data/ migration per D-10"
      min_lines: 60
    - path: scripts/validate-data.rb
      provides: "Full 51-post coverage and tier count validation"
      contains: "validate_blog_inventory"
  key_links:
    - from: lib/tasks/data.rake
      to: "_posts/"
      via: "data:inventory scans markdown posts"
      pattern: "_posts"
    - from: docs/ASTRO-DATA-MAPPING.md
      to: "_data/"
      via: "path mapping table to src/data/"
      pattern: "src/data"
---

## Phase Goal

**As a** maintainer, **I want to** inventory every blog post with curation tiers and document Astro migration mapping, **so that** Phase 2 migration and Phase 4 RSS/index filtering have a complete source of truth.

<objective>
Deliver DATA-04 (blog-inventory.yml), complete validation coverage, and docs/ASTRO-DATA-MAPPING.md per D-10. Final Phase 1 gate — all four DATA requirements pass bundle exec rake data:validate.

Purpose: Blog curation vertical slice + Phase 2 handoff documentation.
Output: _data/blog-inventory.yml (51 posts), docs/ASTRO-DATA-MAPPING.md, enhanced validate-data.rb and data:inventory rake task.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-extraction/01-CONTEXT.md
@.planning/phases/01-foundation-data-extraction/01-RESEARCH.md
@_config.yml
@_posts/
@.planning/phases/01-foundation-data-extraction/01-01-SUMMARY.md
@.planning/phases/01-foundation-data-extraction/01-02-SUMMARY.md
@.planning/phases/01-foundation-data-extraction/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate blog-inventory.yml from _posts scan</name>
  <files>_data/blog-inventory.yml, lib/tasks/data.rake, scripts/validate-data.rb</files>
  <action>
    Per D-13, D-14, D-15, D-16, D-17: generate _data/blog-inventory.yml covering all 51 markdown posts in _posts/ (exclude ea-forge-service.cpp per research).

    Implement data:inventory in lib/tasks/data.rake (replace Plan 01 stub):
    - Glob _posts/*.{md,markdown}
    - Parse frontmatter for title, date, hide, layout
    - Title fallback: frontmatter title, else first markdown H1, else filename-derived slug title (handles 2015 blank titles per Pitfall 4)
    - Compute permalink as /:year/:month/:day/:slug/ matching _config.yml permalink style
    - Assign tier via auto-rules from research: R1 hide:true → archived; R2 Design Document in title → archived; R3 2015 pair with 2024 canonical → archived + canonical_slug; R4 recent technical 2024-2025 → featured candidate; R5 meta posts (welcome, greetings, status update) → archived; R6 remainder → standard
    - Map 10 duplicate pairs: 2015 entries get canonical_slug pointing to 2024 slug id; duplicate_group shared key per topic
    - After auto-rules, manually adjust featured count to 10-15 total (promote best technical posts, demote design docs) per D-15

    YAML structure:
    meta: { generated_at, total_posts: 51, tier_counts: { featured, standard, archived } }
    posts: array of { id, filename, title, pub_date, permalink, tier, auto_rule, hide_frontmatter, canonical_slug, duplicate_group }

    Extend validate_blog_inventory in scripts/validate-data.rb:
    - total_posts must equal 51
    - every _posts markdown file has matching inventory entry
    - all hide:true posts (13) have tier archived
    - featured count between 10 and 15 inclusive
    - archived entries with canonical_slug reference valid id in same file
  </action>
  <verify>
    <automated>bundle exec rake data:inventory && bundle exec ruby -e "require 'yaml'; d=YAML.safe_load(File.read('_data/blog-inventory.yml')); m=d['meta']; abort unless m['total_posts']==51; c=d['posts'].group_by{|p|p['tier']}.transform_values(&:size); abort unless (10..15).cover?(c['featured'].to_i); abort unless d['posts'].count{|p| p['hide_frontmatter']} >= 13"</automated>
  </verify>
  <done>blog-inventory.yml lists 51 posts; 13+ hide:true archived; 10-15 featured; duplicate pairs have canonical_slug; rake data:inventory regenerates file idempotently</done>
</task>

<task type="auto">
  <name>Task 2: Write ASTRO-DATA-MAPPING.md for Phase 2</name>
  <files>docs/ASTRO-DATA-MAPPING.md</files>
  <action>
    Per D-10: document complete Jekyll _data/ to Astro src/data/ mapping for Phase 2 executor. Do NOT scaffold Astro in Phase 1.

    Include:
    - Path mapping table: every _data/ file → src/data/ destination (preserve nested projects/ structure)
    - Astro file() loader registration examples for navigation, social, career, featured projects, archive projects, blog-inventory (reference RESEARCH.md Zod schemas)
    - Liquid site.data.X access patterns → Astro import/getCollection equivalents
    - blog-inventory tier field usage notes for Phase 4 RSS/sitemap/noindex per D-16
    - Note /work/ nav vs projects.md still at /projects/ until Phase 3 permalink change
    - Copy command or migration step list for Phase 2 plan

    Reference actual field names from created YAML files in _data/ (read files, do not invent divergent schemas).
  </action>
  <verify>
    <automated>test -f docs/ASTRO-DATA-MAPPING.md && grep -v '^#' docs/ASTRO-DATA-MAPPING.md | grep -c 'src/data' | awk '{exit ($1>=5)?0:1}'</automated>
  </verify>
  <done>ASTRO-DATA-MAPPING.md exists with path table, loader examples, and Zod schema sketches for all six data files</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 1 validation gate</name>
  <files>scripts/validate-data.rb</files>
  <action>
    Final integration: ensure validate-data.rb --check all validates every _data/ file created in Plans 02-04 without SKIP for required files.

    Confirm:
    - navigation, social, career, featured, archive, blog-inventory all present
    - career.yml no longer has pending_linkedin (if Plan 03 checkpoint completed)
    - Print summary tier_counts from blog-inventory meta on success

    Do NOT wire _includes/header.html to site.data.navigation per D-20.
    Do NOT delete or rename _posts/ files per D-16.
  </action>
  <verify>
    <automated>bundle exec rake data:validate</automated>
  </verify>
  <done>bundle exec rake data:validate exits 0 with all checks passing; Phase 1 DATA-01 through DATA-04 requirements satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| _posts/ frontmatter → inventory YAML | Build-time scan of git-tracked content |
| Inventory tier metadata → future RSS filter | Phase 4 consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | blog-inventory generator | mitigate | Regenerate via rake data:inventory; validate 51-post coverage in CI |
| T-01-04-02 | Repudiation | tier assignment disputes | accept | auto_rule field documents classification rationale |
| T-01-04-SC | Tampering | Package installs | accept | No new packages |
</threat_model>

<verification>
- bundle exec rake data:validate passes with zero failures
- bundle exec rake data:inventory regenerates identical tier_counts (idempotent)
- docs/ASTRO-DATA-MAPPING.md covers all _data/ artifacts
</verification>

<success_criteria>
- DATA-04 satisfied: every blog post has featured/standard/archived tier
- D-10 satisfied: Astro mapping doc ready for Phase 2
- D-14/D-15/D-17 satisfied: hide posts archived, 10-15 featured, duplicates consolidated
- Full Phase 1 gate: all four DATA requirements pass automated validation
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-extraction/01-04-SUMMARY.md` when done
</output>
