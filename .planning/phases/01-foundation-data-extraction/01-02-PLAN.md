---
phase: 01-foundation-data-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - _data/navigation.yml
  - _data/social.yml
  - _data/projects/featured.yml
  - _data/projects/archive.yml
autonomous: true
requirements:
  - DATA-01
  - DATA-03
user_setup: []
must_haves:
  truths:
    - "Maintainer can read canonical nav paths from _data/navigation.yml with lowercase /about/, /work/, /career/, /blog/"
    - "Maintainer can render 3-5 featured project cards from _data/projects/featured.yml with title, summary, tech_stack, github_url, optional demo_url"
    - "Professional repos from portfolio-projects live in archive.yml; UW-CSS academic repos are excluded entirely per D-01/D-03"
  artifacts:
    - path: _data/navigation.yml
      provides: "Canonical primary navigation per D-12, D-18, D-19"
      contains: "/work/"
    - path: _data/social.yml
      provides: "Social and contact links migrated from _config.yml author block per D-12"
      contains: "gravatar_hash"
    - path: _data/projects/featured.yml
      provides: "3-5 hiring-facing featured projects per DATA-01"
      min_lines: 20
    - path: _data/projects/archive.yml
      provides: "Remaining professional repos from portfolio-projects merge per D-02"
      min_lines: 10
  key_links:
    - from: _data/navigation.yml
      to: "/work/"
      via: "url field for Work nav item per D-05"
      pattern: "url: /work/"
    - from: _data/projects/featured.yml
      to: "github.com/tazzledazzle"
      via: "github_url per project"
      pattern: "github_url:"
---

## Phase Goal

**As a** maintainer, **I want to** extract navigation, social links, and curated projects into structured YAML, **so that** Phase 2+ can render hiring pages without re-entering project metadata.

<objective>
Deliver DATA-03 (navigation + social) and DATA-01 (featured + archive projects) as validated Jekyll _data/ files. Merge portfolio-projects content per D-02; exclude UW coursework per D-01/D-03.

Purpose: First vertical data slice — portable hiring project pool and fixed nav config exist before career and blog inventory.
Output: _data/navigation.yml, _data/social.yml, _data/projects/featured.yml, _data/projects/archive.yml passing bundle exec rake data:validate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-extraction/01-CONTEXT.md
@.planning/phases/01-foundation-data-extraction/01-RESEARCH.md
@_config.yml
@projects.md
@.planning/phases/01-foundation-data-extraction/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create navigation.yml and social.yml from _config.yml</name>
  <files>_data/navigation.yml, _data/social.yml</files>
  <action>
    Per D-12, D-18, D-19, D-20: create canonical data files WITHOUT editing _includes/header.html or removing _config.yml navigation block (deferred wiring).

    _data/navigation.yml — exactly 4 entries ordered by order field:
    - About → /about/ (order 1)
    - Work → /work/ (order 2) per D-05 — NOT /projects/
    - Career → /career/ (order 3)
    - Blog → /blog/ (order 4) — fixes _config.yml typo url '/Blog'

    _data/social.yml — migrate values from _config.yml author block + gravatar_hash:
    - name, email
    - github: { username, url }
    - linkedin: { username, url }
    - twitter: { username, url }
    - gravatar_hash

    Do NOT put navigation or social back into _config.yml — single source is _data/ per D-12.
  </action>
  <verify>
    <automated>bundle exec rake data:validate 2>&1 | grep -E 'navigation.*PASS|social.*PASS' | wc -l | awk '{exit ($1>=2)?0:1}'</automated>
  </verify>
  <done>navigation.yml has 4 lowercase paths including /blog/ and /work/; social.yml contains all migrated contact fields; data:validate passes navigation and social checks</done>
</task>

<task type="auto">
  <name>Task 2: Build featured.yml from projects.md and portfolio-projects</name>
  <files>_data/projects/featured.yml</files>
  <action>
    Per D-01, D-02, D-04, D-11: create _data/projects/featured.yml with exactly 3-5 personal/professional repos (no UW-CSS).

    Required entries (minimum):
    1. native-macos-log-analyzer — from projects.md; demo_status code_only unless live demo confirmed
    2. imgannotator — from projects.md; set demo_url and demo_status per actual availability (D-04 mixed)

    Add 1-3 from portfolio-projects README (fetch https://raw.githubusercontent.com/tazzledazzle/portfolio-projects/main/README.md or clone table manually if offline). Default discretion picks per research: ws-chat-fast, online-bookstore, otel-demo-stack — override only if README shows broken status.

    Each entry schema: id (kebab-case slug), title, summary (1-2 sentences hiring-focused), tech_stack (array), github_url, demo_url (null if none), demo_status (live|code_only|broken), featured: true, source (projects.md|portfolio-projects).

    Exclude ALL github URLs containing github.com/tazzledazzle/UW-CSS per D-03.
  </action>
  <verify>
    <automated>bundle exec ruby -e "require 'yaml'; d=YAML.safe_load(File.read('_data/projects/featured.yml')); abort('count') unless (3..5).cover?(d.size); abort('uw') if d.any?{|p| p['github_url'].to_s.include?('UW-CSS')}"</automated>
  </verify>
  <done>featured.yml has 3-5 entries with all required fields; no academic repos; data:validate projects check passes for featured</done>
</task>

<task type="auto">
  <name>Task 3: Build archive.yml from remaining portfolio-projects repos</name>
  <files>_data/projects/archive.yml</files>
  <action>
    Per D-02, D-11: populate _data/projects/archive.yml with remaining professional repos from portfolio-projects README table NOT already in featured.yml.

    Same field schema as featured minus featured flag (or featured: false). Include repos with status stable/WIP from README; skip UW/academic entries entirely.

    Absorb portfolio-projects content intent: data replaces outbound "Projects Site Here" link — do NOT edit projects.md in this plan (Phase 3 removes link).

    Set demo_url null and demo_status code_only where no live demo per D-04.
  </action>
  <verify>
    <automated>bundle exec rake data:validate 2>&1 | tail -3</automated>
  </verify>
  <done>archive.yml contains professional repos only; no overlap with featured ids; bundle exec rake data:validate passes all project checks</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw README → local YAML | External content ingested at author time |
| _data/projects/*.yml → Jekyll site.data | Build-time data layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | github_url in project YAML | mitigate | validate-data.rb enforces URL format; reject non-github URLs for github_url field |
| T-01-02-02 | Tampering | portfolio-projects README fetch | mitigate | Verify source host is raw.githubusercontent.com/tazzledazzle/portfolio-projects |
| T-01-02-SC | Tampering | npm/pip installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- bundle exec rake data:validate passes navigation, social, and projects checks
- featured.yml count between 3 and 5; zero UW-CSS URLs across both project files
</verification>

<success_criteria>
- DATA-03 satisfied: navigation.yml and social.yml are canonical single source in _data/
- DATA-01 satisfied: featured and archive YAML contain required fields for all curated projects
- D-05 reflected: nav points to /work/ not /projects/
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-extraction/01-02-SUMMARY.md` when done
</output>
