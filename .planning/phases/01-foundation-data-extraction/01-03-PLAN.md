---
phase: 01-foundation-data-extraction
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - _data/career.yml
autonomous: false
requirements:
  - DATA-02
user_setup: []
must_haves:
  truths:
    - "Maintainer can render a career timeline from _data/career.yml with employer, role, dates, and achievement bullets"
    - "Career data includes last 3 roles only with at least one quantified metric bullet per role"
    - "Education lists UW Bothell CS degree only without coursework list"
  artifacts:
    - path: _data/career.yml
      provides: "LinkedIn-sourced career structured data per DATA-02"
      contains: "roles:"
  key_links:
    - from: _data/career.yml
      to: "LinkedIn user input"
      via: "checkpoint population per D-06"
      pattern: "status:"
---

## Phase Goal

**As a** maintainer, **I want to** store real career experience in structured YAML, **so that** Phase 3 can render a credible hiring timeline with quantified achievements.

<objective>
Deliver DATA-02: _data/career.yml with last 3 LinkedIn roles, quantified bullets, and degree-only education. Blocked on user LinkedIn paste per D-06 — ship schema first, populate at checkpoint.

Purpose: Career vertical slice — replaces placeholder career.md content with portable structured data.
Output: _data/career.yml passing validation with status field removed after user input.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-extraction/01-CONTEXT.md
@.planning/phases/01-foundation-data-extraction/01-RESEARCH.md
@career.md
@.planning/phases/01-foundation-data-extraction/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create career.yml schema with pending_linkedin placeholders</name>
  <files>_data/career.yml</files>
  <action>
    Per D-06, D-07, D-08, D-09: create _data/career.yml with valid structure before user input.

    Top-level fields:
    - status: pending_linkedin (removed after Task 2 checkpoint completes)
    - education: single entry — institution "University of Washington Bothell", degree "Bachelor of Science in Computer Science & Software Engineering" — NO coursework array per D-09
    - roles: exactly 3 placeholder entries (role-1, role-2, role-3) with empty employer, title, start_date, end_date, bullets: [""] to satisfy array shape

    Do NOT copy generic placeholder text from career.md ("Architected cloud-native applications") into final bullets — those are anti-patterns per research Pitfall 6.

    Do NOT edit career.md in this plan — data file is canonical source for Phase 3.
  </action>
  <verify>
    <automated>test -f _data/career.yml && bundle exec ruby -e "require 'yaml'; d=YAML.safe_load(File.read('_data/career.yml')); abort unless d['roles'].size==3 && d['education'].size>=1"</automated>
  </verify>
  <done>career.yml exists with 3 role slots, education degree only, status pending_linkedin; validator accepts schema with pending status</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Populate career.yml from LinkedIn paste</name>
  <files>_data/career.yml</files>
  <action>
    Per D-06: request user paste LinkedIn career details (last 3 roles: employer, title, start/end dates, 2-4 achievement bullets each).

    On resume after user provides data:
    - Fill all 3 roles (most recent first) per D-07
    - Ensure each role has at least one bullet containing a quantified metric (digit, %, or measurable outcome) per D-08
    - Remove status: pending_linkedin key entirely once populated
    - Remove empty placeholder bullets
    - Run bundle exec rake data:validate — must pass career checks

    If user provides fewer than 3 roles, document gap in SUMMARY and leave unfilled slots with explicit TODO comment in YAML (not empty strings).
  </action>
  <how-to-verify>
    1. Open _data/career.yml — confirm 3 roles with real employer names (or documented TODOs)
    2. Each role has dates and at least one quantified bullet
    3. Education has degree only, no coursework list
    4. Run: bundle exec rake data:validate — career section passes
  </how-to-verify>
  <resume-signal>Paste LinkedIn details for last 3 roles, or type "approved" if career.yml already populated correctly</resume-signal>
  <verify>
    <automated>bundle exec rake data:validate 2>&1 | grep -v '^#' | grep -c career || bundle exec ruby -e "require 'yaml'; d=YAML.safe_load(File.read('_data/career.yml')); abort if d.key?('status') && d['status']=='pending_linkedin'; abort unless d['roles'].all?{|r| r['employer'].to_s.strip!='' }"</automated>
  </verify>
  <done>career.yml has real LinkedIn-sourced data for up to 3 roles; no pending_linkedin status; data:validate career checks pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User paste → career.yml | Maintainer-provided professional history stored in repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information disclosure | career.yml employer details | accept | Public hiring site content; no secrets expected |
| T-01-03-02 | Tampering | career.yml manual edit | mitigate | validate-data.rb enforces required fields and bullet count |
| T-01-03-SC | Tampering | External packages | accept | No packages in this plan |
</threat_model>

<verification>
- career.yml validates with bundle exec rake data:validate
- No coursework list in education section
- Each role has ≥1 quantified metric bullet when status is not pending
</verification>

<success_criteria>
- DATA-02 satisfied with real employer names, roles, dates, and achievement bullets
- D-06 checkpoint completed with user LinkedIn input
- D-07/D-08/D-09 constraints enforced in final YAML
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-extraction/01-03-SUMMARY.md` when done
</output>
