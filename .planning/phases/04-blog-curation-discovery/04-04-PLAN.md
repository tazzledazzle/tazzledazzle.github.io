---
phase: 04-blog-curation-discovery
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/blog/BlogPostListItem.astro
  - src/pages/blog/index.astro
  - src/styles/global.css
autonomous: true
requirements: [BLOG-01]
must_haves:
  truths:
    - "Blog index shows Featured section heading with featured-tier posts at top per D-61"
    - "Standard-tier posts appear as separate chronological list below featured per D-62"
    - "Index entries show publication date and estimated read time per D-63"
    - "Index entries show description from frontmatter when present; no auto-excerpt per D-64"
    - "Archived posts do not appear anywhere on /blog/ index per D-16, BLOG-01"
    - "Blog index links to /blog/archive/ for archive discovery per D-80"
  artifacts:
    - path: "src/pages/blog/index.astro"
      provides: "Tiered featured + standard blog index"
      contains: "Featured"
    - path: "src/components/blog/BlogPostListItem.astro"
      provides: "Index row with date, read time, optional description"
  key_links:
    - from: "src/pages/blog/index.astro"
      to: "src/lib/blog/posts.ts"
      via: "getFeaturedPosts and getStandardPosts"
      pattern: "getFeaturedPosts|getStandardPosts"
    - from: "src/pages/blog/index.astro"
      to: "/blog/archive/"
      via: "archive discovery link"
      pattern: "/blog/archive/"
---

## Phase Goal

**As an** engineering peer, **I want to** discover and read curated technical writing, **so that** legacy noise doesn't dilute signal.

<objective>
Upgrade /blog/ index from flat list to tiered Featured + Standard sections with metadata-rich entries and archive link.

Purpose: Satisfy BLOG-01 curation visibility (D-61–D-64, D-80).
Output: BlogPostListItem component and tiered blog/index.astro.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-blog-curation-discovery/04-CONTEXT.md
@src/pages/blog/index.astro
@src/lib/blog/posts.ts
@src/lib/blog/readTime.ts
@src/lib/blog/dates.ts
@src/data/blog-inventory.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlogPostListItem component</name>
  <files>src/components/blog/BlogPostListItem.astro, src/styles/global.css</files>
  <action>
    Create BlogPostListItem.astro accepting: title, permalink, pubDate (Date), readTimeMinutes (number), description?: string.

    Render linked title (h2 or strong inside anchor to permalink). Show short index date (ISO or abbreviated — index may use shorter format than post long date; post pages use long format per D-65) plus "{n} min read" per D-63.

    When description prop is provided and non-empty, render as p.blog-post-description (D-64). Do NOT auto-generate excerpt from body when description missing — omit description block entirely (D-64).

    Minimal list item styling in global.css (spacing, meta text color).
  </action>
  <verify>
    <automated>npm run build && test -f src/components/blog/BlogPostListItem.astro</automated>
  </verify>
  <done>Reusable index row component shows date, read time, optional description.</done>
</task>

<task type="auto">
  <name>Task 2: Tiered blog index with featured and standard sections</name>
  <files>src/pages/blog/index.astro</files>
  <action>
    Rewrite src/pages/blog/index.astro:

    Load posts via getCollection('blog'). Split with getFeaturedPosts and getStandardPosts from src/lib/blog/posts.ts (D-61, D-62). Do not include archived posts anywhere (D-16).

    Render page h1 "Blog", then section with h2 "Featured" listing featured posts via BlogPostListItem (newest first within featured per discretion). Then section with h2 "All posts" or "Recent" listing standard-tier posts chronologically (D-62).

    For each item compute readTimeMinutes from post.body via calculateReadTime. Pass post.data.description only when defined (D-64).

    Add paragraph or nav link "Browse archive →" pointing to /blog/archive/ (D-80). Acceptable as inline link below lists — no global nav change (deferred to Phase 5).

    Verify dist/blog/index.html: contains "Featured", featured post title from inventory, does NOT contain a known archived-only post title, includes /blog/archive/ link.
  </action>
  <verify>
    <automated>npm run build && grep -q 'Featured' dist/blog/index.html && grep -q '/blog/archive/' dist/blog/index.html && ! grep -q 'Welcome to Jekyll' dist/blog/index.html</automated>
  </verify>
  <done>Tiered blog index ships with featured prominence and archived exclusion (BLOG-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blog index → public HTML | Static curated listing only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | archived post titles on index | mitigate | Filter tier !== archived in getFeaturedPosts/getStandardPosts |
| T-04-SC | Tampering | npm packages | accept | No new packages |
</threat_model>

<verification>
dist/blog/index.html shows Featured section, standard list, archive link; no archived titles.
</verification>

<success_criteria>
Blog index reflects curation tiers with metadata-rich entries per D-61–D-64.
</success_criteria>

<output>
Create `.planning/phases/04-blog-curation-discovery/04-04-SUMMARY.md` when done
</output>
