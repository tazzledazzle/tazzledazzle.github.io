---
phase: 04-blog-curation-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - src/layouts/MainLayout.astro
autonomous: false
requirements: [BLOG-03, BLOG-07]
user_setup: []
must_haves:
  truths:
    - "Code blocks in blog posts render with Expressive Code frames and GitHub Dark theme per D-69, D-71"
    - "Fenced blocks with language identifiers show language label top-right per D-70"
    - "Legacy ```math fences render as plain pre/code without KaTeX per D-72"
    - "Every built page head includes RSS autodiscovery link to /rss.xml per D-83, D-84"
  artifacts:
    - path: "astro.config.mjs"
      provides: "astro-expressive-code integration before mdx()"
      contains: "astro-expressive-code"
    - path: "src/layouts/MainLayout.astro"
      provides: "RSS alternate link in head on all pages"
      contains: 'rel="alternate"'
  key_links:
    - from: "astro.config.mjs"
      to: "markdown/MDX code blocks"
      via: "expressiveCode integration order before mdx()"
      pattern: "expressiveCode|astro-expressive-code"
    - from: "src/layouts/MainLayout.astro"
      to: "/rss.xml"
      via: "link rel=alternate type=application/rss+xml"
      pattern: 'href="/rss.xml"'
---

## Phase Goal

**As an** engineering peer, **I want to** discover and read curated technical writing, **so that** legacy noise doesn't dilute signal.

<objective>
Install Expressive Code for syntax-highlighted code blocks and add sitewide RSS autodiscovery in MainLayout.

Purpose: Satisfy BLOG-03 (D-69–D-72) and BLOG-07 (D-83–D-84) before post layout work depends on rendered code blocks.
Output: astro-expressive-code configured with GitHub Dark + top-right language labels; RSS link in every page head.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-blog-curation-discovery/04-CONTEXT.md
@astro.config.mjs
@src/layouts/MainLayout.astro
@src/pages/rss.xml.ts
@src/content/blog/2025-01-08-cloud-comp.md
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify astro-expressive-code package legitimacy</name>
  <files>package.json</files>
  <action>
    No RESEARCH.md Package Legitimacy Audit exists for Phase 4. Before install, human verifies astro-expressive-code on npmjs.com: publisher expressive-code org, ~900+ GitHub stars, official Expressive Code Astro integration. Confirm package name is exactly astro-expressive-code (not typosquat). Reply "approved" to proceed with install in Task 2.
  </action>
  <verify>
    <automated>test -f package.json</automated>
  </verify>
  <done>Maintainer approved astro-expressive-code as [ASSUMED] legitimate dependency.</done>
  <resume-signal>Type "approved" after verifying npm package, or name an alternative if blocked.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install and configure Expressive Code with GitHub Dark theme</name>
  <files>package.json, package-lock.json, astro.config.mjs</files>
  <action>
    Install astro-expressive-code after Task 1 approval. Update astro.config.mjs:

    Import astroExpressiveCode from 'astro-expressive-code'. Place the integration BEFORE mdx() in the integrations array (required ordering).

    Configure per D-69, D-71: themes: ['github-dark'] only (site already dark baseline per D-38; single dark theme avoids light/dark code toggle).

    Configure per D-70: enable language labels on code frames with top-right placement via expressiveCode options — set frames.showLanguageLabel true and use styleOverrides.frames to position language label top-right (e.g., languageLabelPosition or equivalent EC frame config documented at expressive-code.com).

    Per D-72: do NOT add KaTeX or math plugins. ```math fences without a highlighter mapping should remain unhighlighted plain code blocks — verify cloud-comp post math fence is not broken.

    Per Claude discretion: omit copy button and line numbers unless trivial to enable; not required by locked decisions.

    Run npm run build and confirm dist output includes expressive code CSS asset (/_astro/ec.*.css pattern).
  </action>
  <verify>
    <automated>npm run build && ls dist/_astro/ec*.css 2>/dev/null | head -1 | grep -q ec</automated>
  </verify>
  <done>Build succeeds with Expressive Code assets; GitHub Dark theme active on fenced code blocks.</done>
</task>

<task type="auto">
  <name>Task 3: Add RSS autodiscovery link to MainLayout head</name>
  <files>src/layouts/MainLayout.astro</files>
  <action>
    Update MainLayout.astro head section to include sitewide RSS autodiscovery per D-83 and D-84:

    link rel="alternate" type="application/rss+xml" title="Terence Schumacher Blog" href="/rss.xml"

  Place after charset/viewport meta tags. Use root-relative /rss.xml (existing Phase 2 endpoint). Do not add feed.xml alias (deferred). Link must appear on every page using MainLayout — no per-page opt-in.

  Verify on built homepage, a blog post, and /blog/ index HTML.
  </action>
  <verify>
    <automated>npm run build && grep -q 'rel="alternate".*application/rss\+xml' dist/index.html && grep -q 'href="/rss.xml"' dist/index.html && grep -q 'href="/rss.xml"' dist/blog/index.html</automated>
  </verify>
  <done>RSS autodiscovery present in head of all MainLayout pages (BLOG-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → astro-expressive-code | Third-party package installed at build time |
| RSS link → /rss.xml | Static path to existing first-party feed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | astro-expressive-code npm package | mitigate | Blocking human checkpoint Task 1 verifies publisher before install |
| T-04-04 | Spoofing | RSS autodiscovery href | accept | First-party /rss.xml; no external feed URL |
| T-04-SC | Tampering | npm install | mitigate | slopcheck + blocking checkpoint for [ASSUMED] astro-expressive-code |
</threat_model>

<verification>
npm run build passes; EC CSS emitted; RSS link in dist HTML for homepage and blog routes.
</verification>

<success_criteria>
Expressive Code renders on posts with GitHub Dark theme and language labels; RSS autodiscovery on all pages.
</success_criteria>

<output>
Create `.planning/phases/04-blog-curation-discovery/04-02-SUMMARY.md` when done
</output>
