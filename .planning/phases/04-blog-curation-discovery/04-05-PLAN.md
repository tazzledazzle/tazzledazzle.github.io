---
phase: 04-blog-curation-discovery
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-04]
files_modified:
  - src/pages/blog/tags/[tag].astro
  - src/pages/blog/archive/index.astro
  - src/styles/global.css
autonomous: true
requirements: [BLOG-04, BLOG-05]
must_haves:
  truths:
    - "Tag index pages exist at /blog/tags/{tag-slug}/ per D-73"
    - "Tag indexes list featured and standard posts only; archived excluded per D-74"
    - "Archive page at /blog/archive/ groups discoverable posts under year headings per D-77, D-79"
    - "Archive includes featured and standard posts only per D-78"
    - "Archive page is linked from /blog/ index per D-80"
  artifacts:
    - path: "src/pages/blog/tags/[tag].astro"
      provides: "Static paths per unique tag slug"
    - path: "src/pages/blog/archive/index.astro"
      provides: "Year-sectioned archive listing"
  key_links:
    - from: "src/pages/blog/tags/[tag].astro"
      to: "src/lib/blog/posts.ts"
      via: "getAllTags and discoverable filter"
      pattern: "getAllTags|getDiscoverablePosts"
    - from: "src/pages/blog/archive/index.astro"
      to: "src/components/blog/BlogPostListItem.astro"
      via: "year-grouped post lists"
      pattern: "BlogPostListItem"
---

## Phase Goal

**As an** engineering peer, **I want to** discover and read curated technical writing, **so that** legacy noise doesn't dilute signal.

<objective>
Add tag index routes and year-grouped archive page for blog discovery beyond the main index.

Purpose: Satisfy BLOG-04 and BLOG-05 (D-73–D-80).
Output: /blog/tags/[tag].astro and /blog/archive/index.astro.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-blog-curation-discovery/04-CONTEXT.md
@src/lib/blog/posts.ts
@src/lib/blog/tags.ts
@src/components/blog/BlogPostListItem.astro
@src/pages/blog/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag index pages at /blog/tags/{slug}/</name>
  <files>src/pages/blog/tags/[tag].astro</files>
  <action>
    Create src/pages/blog/tags/[tag].astro with getStaticPaths:

    Load all blog posts, filter to discoverable (featured + standard) per D-74. Build unique tag slug set via getAllTags + tagToSlug (D-73).

    For each slug, return params { tag: slug } and props { displayName, posts } where posts match that slug (compare tagToSlug(tag) === slug).

    Page renders MainLayout with title "Posts tagged {displayName}", h1, and BlogPostListItem list sorted pubDate desc. Show empty state message when no posts (should not happen for generated paths).

    Do NOT create sitewide /tags/ index — per-tag pages only (Claude discretion, D-75 scope).

    Verify dist/blog/tags/algorithms/index.html builds when algorithms tag exists post-backfill.
  </action>
  <verify>
    <automated>npm run build && ls dist/blog/tags/*/index.html 2>/dev/null | head -1 | grep -q index.html</automated>
  </verify>
  <done>Tag routes generate for each unique discoverable tag slug (BLOG-04, D-73, D-74).</done>
</task>

<task type="auto">
  <name>Task 2: Year archive page at /blog/archive/</name>
  <files>src/pages/blog/archive/index.astro, src/styles/global.css</files>
  <action>
    Create src/pages/blog/archive/index.astro:

    Load discoverable posts via getDiscoverablePosts (D-78). Group by pubDate full year (getFullYear()). Sort years descending. Within each year, sort posts pubDate descending.

    Render h1 "Archive", then for each year an h2 with year number and ul of BlogPostListItem entries (D-77, D-79). Reuse BlogPostListItem from 04-04 for consistency (date, read time, optional description).

    Add link back to /blog/ at top or bottom.

    Ensure /blog/index.astro archive link from 04-04 targets this page (D-80) — update if missing.

    Verify dist/blog/archive/index.html contains at least one h2 year heading (e.g., 2025) and excludes archived-only content.
  </action>
  <verify>
    <automated>npm run build && grep -qE '>(2024|2025|2023)<' dist/blog/archive/index.html && grep -q '/blog/' dist/blog/archive/index.html</automated>
  </verify>
  <done>Single archive page groups discoverable posts by year (BLOG-05, D-77–D-80).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tag slug URL param | Normalized via tagToSlug at build time only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | [tag] param | mitigate | Static paths generated at build; only known slugs emitted |
| T-04-09 | Information Disclosure | archived on tag/archive | mitigate | getDiscoverablePosts excludes archived tier |
| T-04-SC | Tampering | npm packages | accept | No new packages |
</threat_model>

<verification>
Tag and archive routes build; year sections present; archived posts absent from discovery pages.
</verification>

<success_criteria>
Readers can browse by tag and by year archive for curated posts only.
</success_criteria>

<output>
Create `.planning/phases/04-blog-curation-discovery/04-05-SUMMARY.md` when done
</output>
