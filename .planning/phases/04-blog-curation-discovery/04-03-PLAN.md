---
phase: 04-blog-curation-discovery
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/layouts/PostLayout.astro
  - src/components/blog/TagList.astro
  - src/components/blog/PostNav.astro
  - src/pages/[...slug].astro
  - src/styles/global.css
autonomous: true
requirements: [BLOG-02, BLOG-04, BLOG-06]
must_haves:
  truths:
    - "Post pages show long-format publication date per D-65"
    - "Post pages show estimated read time from 225 WPM per D-66"
    - "Archived post pages include noindex meta and visible Archived post banner per D-67"
    - "Archived posts remain fully readable with complete body content per D-68"
    - "Post footer shows clickable tags linking to /blog/tags/{slug}/ per D-75"
    - "Post bottom shows prev/next links with titles for discoverable sequence only per D-81, D-82"
  artifacts:
    - path: "src/layouts/PostLayout.astro"
      provides: "Post metadata header, archived banner slot, footer tags, PostNav"
    - path: "src/components/blog/TagList.astro"
      provides: "Clickable tag links to tag index routes"
    - path: "src/components/blog/PostNav.astro"
      provides: "Previous/Next navigation with titles"
    - path: "src/pages/[...slug].astro"
      provides: "Wired post page using PostLayout and prev/next"
  key_links:
    - from: "src/pages/[...slug].astro"
      to: "src/lib/blog/posts.ts"
      via: "getPrevNextPosts and calculateReadTime"
      pattern: "getPrevNextPosts|calculateReadTime"
    - from: "src/layouts/PostLayout.astro"
      to: "/blog/tags/"
      via: "TagList tagToSlug href"
      pattern: "/blog/tags/"
    - from: "src/pages/[...slug].astro"
      to: "noindex meta"
      via: "archived tier conditional head"
      pattern: "noindex"
---

## Phase Goal

**As an** engineering peer, **I want to** discover and read curated technical writing, **so that** legacy noise doesn't dilute signal.

<objective>
Build PostLayout and upgrade the permalink post template with metadata, archived handling, footer tags, and prev/next navigation.

Purpose: Deliver the core reading experience for BLOG-02 and BLOG-06 plus tag footer links (D-65–D-68, D-75, D-81–D-82).
Output: PostLayout.astro, TagList, PostNav, and updated [...slug].astro.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-blog-curation-discovery/04-CONTEXT.md
@src/pages/[...slug].astro
@src/layouts/MainLayout.astro
@src/lib/blog/dates.ts
@src/lib/blog/readTime.ts
@src/lib/blog/tags.ts
@src/lib/blog/posts.ts
@src/content/blog/2025-01-08-cloud-comp.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostLayout with metadata and archived banner</name>
  <files>src/layouts/PostLayout.astro, src/styles/global.css</files>
  <action>
    Create src/layouts/PostLayout.astro accepting props: title, pubDate (Date), readTimeMinutes (number), tags (string[]), tier, isArchived (boolean), prev/next post summaries (nullable { title, permalink }).

    Render article header with h1, long date via formatLongDate (D-65), and read time label e.g. "5 min read" from readTimeMinutes (D-66).

    When isArchived true, render visible banner at top of article: "Archived post" with subdued styling (D-67). Content slot renders full post body below banner — no truncation (D-68).

    Extend global.css minimally for post header meta row, archived banner, and article spacing within existing CSS variables — no Phase 5 typography tokens.

    PostLayout wraps MainLayout. For archived posts, pass a noindex flag so [...slug].astro can inject meta name="robots" content="noindex" in head via MainLayout extension or Astro head slot pattern (D-67). Prefer extending MainLayout with optional robots?: string prop rather than duplicating layout.
  </action>
  <verify>
    <automated>npm run build && test -f dist/2025/01/08/cloud-computing-notes/index.html && grep -qE 'January|February|March|April|May|June|July|August|September|October|November|December' dist/2025/01/08/cloud-computing-notes/index.html</automated>
  </verify>
  <done>PostLayout renders long-format date, read time, and archived banner; supports noindex head injection.</done>
</task>

<task type="auto">
  <name>Task 2: TagList and PostNav components</name>
  <files>src/components/blog/TagList.astro, src/components/blog/PostNav.astro</files>
  <action>
    Create TagList.astro: accept tags: string[]. Map each tag to anchor href={`/blog/tags/${tagToSlug(tag)}/`} with visible tag label (D-75). Omit component when tags array empty. Use nav or ul with class post-tags for verification.

    Create PostNav.astro: accept prev and next nullable { title, permalink }. Render bottom nav per D-82 with visible post titles: left link text `← Previous: {prev.title}` with href={prev.permalink}; right link text `{next.title} → Next` with href={next.permalink}. Use flex row space-between. Omit side when null. Class post-nav for verification.

    Styling: minimal Tailwind/utilities consistent with Phase 3 shell (D-38).
  </action>
  <verify>
    <automated>grep -q 'Previous:' src/components/blog/PostNav.astro && grep -q 'post-tags' src/components/blog/TagList.astro</automated>
  </verify>
  <done>Reusable tag footer and prev/next components with visible post titles ready for PostLayout wiring (D-82).</done>
</task>

<task type="auto">
  <name>Task 3: Wire [...slug].astro to PostLayout with prev/next and noindex</name>
  <files>src/pages/[...slug].astro, src/layouts/MainLayout.astro, src/layouts/PostLayout.astro</files>
  <action>
    Update src/pages/[...slug].astro:

    Load all blog posts via getCollection('blog'). Compute discoverable list and { prev, next } via getPrevNextPosts(posts, post.data.permalink) (D-81).

    Calculate read time from post.body via calculateReadTime (D-66).

    Resolve tags via resolvePostTags(post.data.tags) after backfill (D-76).

    Replace inline MainLayout/article markup with PostLayout wrapping rendered Content.

    Pass isArchived = post.data.tier === 'archived'. Inject robots noindex in head when archived (D-67) while keeping full Content rendered (D-68).

    Extend MainLayout.astro with optional robots?: string prop forwarded to meta robots tag when set.

    Set page title and optional description from post frontmatter.

    Verify built HTML for a featured post contains long date pattern, min read, post-tags links, post-nav. Verify an archived post dist HTML contains noindex and Archived post banner text.
  </action>
  <verify>
    <automated>npm run build && ARCHIVED=$(rg -l 'tier: "archived"' src/content/blog | head -1 | xargs basename -s .md) && PERMALINK=$(rg "permalink:" "src/content/blog/${ARCHIVED}.md" | head -1 | sed 's/.*"\(.*\)".*/\1/') && SLUG=$(echo "$PERMALINK" | sed 's#^/##;s#/$##') && grep -q 'Archived post' "dist/${SLUG}/index.html" && grep -q 'noindex' "dist/${SLUG}/index.html"</automated>
  </verify>
  <done>All permalink post pages use PostLayout with metadata, tags, prev/next, and archived compliance (BLOG-02, BLOG-06, D-75).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Post markdown body → readTime | Local static content processing |
| Tag hrefs → /blog/tags/{slug}/ | Internal routes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | archived noindex | mitigate | robots noindex on archived tier only (D-67) |
| T-04-06 | Tampering | tag slug in URL | mitigate | tagToSlug normalizes to safe lowercase hyphenated slugs |
| T-04-SC | Tampering | npm packages | accept | No new packages in this plan |
</threat_model>

<verification>
Build passes; featured post shows metadata and nav; archived post shows banner + noindex with full content.
</verification>

<success_criteria>
Post reading experience complete: date, read time, code (via 04-02), tags, prev/next, archived handling.
</success_criteria>

<output>
Create `.planning/phases/04-blog-curation-discovery/04-03-SUMMARY.md` when done
</output>
