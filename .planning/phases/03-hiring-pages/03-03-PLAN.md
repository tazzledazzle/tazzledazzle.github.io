---
phase: 03-hiring-pages
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/pages/work.astro
  - src/components/ArchiveProjects.astro
autonomous: true
requirements: [HIRE-03, HIRE-04]
must_haves:
  truths:
    - "Work page shows 3-5 featured projects in responsive card grid (2 col desktop, 1 col mobile) per D-49"
    - "Each card shows impact summary, tech pill tags, and GitHub link per D-50 and HIRE-03"
    - "Demo links appear only when demo_url is set; code_only projects omit demo UI silently per D-51 and HIRE-04"
    - "Collapsed More projects section lists archive.yml entries below featured grid per D-52"
    - "/work/ remains canonical projects URL per D-05"
  artifacts:
    - path: "src/pages/work.astro"
      provides: "Featured grid + archive section hiring page"
    - path: "src/components/ArchiveProjects.astro"
      provides: "Collapsible archive project list"
  key_links:
    - from: "src/pages/work.astro"
      to: "src/data/projects/featured.yml"
      via: "getCollection featuredProjects"
      pattern: "featuredProjects"
    - from: "src/pages/work.astro"
      to: "src/data/projects/archive.yml"
      via: "getCollection archiveProjects"
      pattern: "archiveProjects"
    - from: "src/pages/work.astro"
      to: "src/components/ProjectCard.astro"
      via: "grid map"
      pattern: "ProjectCard"
---

<objective>
Upgrade /work/ from a minimal list into a hiring-grade project showcase with featured cards and a collapsible archive section.

Purpose: Deliver HIRE-03 (featured project cards) and HIRE-04 (demo link handling) on the canonical projects URL.
Output: work.astro grid layout and ArchiveProjects collapsible section.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hiring-pages/03-CONTEXT.md
@src/data/projects/featured.yml
@src/data/projects/archive.yml
@src/pages/work.astro
@src/components/ProjectCard.astro
@.planning/phases/03-hiring-pages/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render featured project card grid on /work/</name>
  <files>src/pages/work.astro</files>
  <action>
    Replace work.astro minimal ul/li with a responsive CSS grid: grid-cols-1 below md, grid-cols-2 at md+ per D-49. Load all featuredProjects via getCollection and map each to ProjectCard from 03-01. Remove @ts-nocheck by using proper collection entry types.

    Page title remains "Work | Terence Schumacher". Add brief page intro line orienting recruiters to featured work. Verify all 5 featured projects render with summary, tech_stack pills, and GitHub links per HIRE-03.

    Confirm no demo links render for current data where demo_url is null (all featured are code_only per CONTEXT) per D-51 — absence is the correct HIRE-04 state, not an error.
  </action>
  <verify>
    <automated>npm run build && test -f dist/work/index.html && grep -c 'github.com/tazzledazzle' dist/work/index.html | awk '{ if ($1 >= 5) exit 0; exit 1 }' && grep -q 'grid' dist/work/index.html</automated>
  </verify>
  <done>Work page displays all featured projects in a 2-column desktop / 1-column mobile card grid with tech tags and GitHub links.</done>
</task>

<task type="auto">
  <name>Task 2: Add collapsed More projects archive section</name>
  <files>src/components/ArchiveProjects.astro, src/pages/work.astro</files>
  <action>
    Create ArchiveProjects.astro loading getCollection('archiveProjects'). Wrap in a native details/summary element titled "More projects" defaulting to collapsed on first load (Claude discretion per CONTEXT) per D-52. Inside, render archive entries using ProjectCard or a lighter list variant with same D-51 demo rules.

    Import and place below featured grid in work.astro. Ensure archive section is keyboard accessible and does not break mobile layout.

    Do not add deep case study pages or per-project routes — out of scope.
  </action>
  <verify>
    <automated>npm run build && grep -q 'More projects' dist/work/index.html && grep -q 'Project Generator\|RabbitMQ Demo' dist/work/index.html</automated>
  </verify>
  <done>Work page shows featured grid plus collapsible archive section sourced from archive.yml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Outbound project URLs | GitHub and optional demo links leave site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Spoofing | ProjectCard outbound links | mitigate | URLs from Zod-validated YAML at build time only |
| T-03-07 | Information disclosure | archive.yml content | accept | Public portfolio data by design |
</threat_model>

<verification>
dist/work/index.html contains 5+ GitHub links, grid layout, "More projects" section, and at least one archive project title.
</verification>

<success_criteria>
Recruiter browsing /work/ sees featured card grid with tech tags and GitHub links; archive projects available in collapsed section; no spurious demo links for code_only projects.
</success_criteria>

<output>
Create `.planning/phases/03-hiring-pages/03-03-SUMMARY.md` when done
</output>
