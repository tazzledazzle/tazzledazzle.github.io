---
phase: 03-hiring-pages
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/pages/career.astro
  - src/pages/about.astro
  - src/components/CareerTimeline.astro
  - src/lib/formatCareerDate.ts
autonomous: true
requirements: [HIRE-05]
must_haves:
  truths:
    - "Career page renders vertical timeline with date rail left, content right per D-53"
    - "All achievement bullets display per role with no truncation per D-54"
    - "Education section appears below roles showing UW CS degree per D-55 and D-09"
    - "Employment dates show human-readable month and year from ISO storage per D-56"
    - "About page has 2-3 paragraph bio plus Connect section with GitHub, LinkedIn, and email links per D-60"
  artifacts:
    - path: "src/lib/formatCareerDate.ts"
      provides: "YYYY-MM to Jan 2025 / Present formatter"
    - path: "src/components/CareerTimeline.astro"
      provides: "Vertical timeline for roles + education block"
    - path: "src/pages/career.astro"
      provides: "Career hiring page"
    - path: "src/pages/about.astro"
      provides: "Expanded about bio page"
  key_links:
    - from: "src/pages/career.astro"
      to: "src/data/career.yml"
      via: "getEntry career default"
      pattern: "getEntry\\(['\"]career"
    - from: "src/components/CareerTimeline.astro"
      to: "src/lib/formatCareerDate.ts"
      via: "date formatting for start/end"
      pattern: "formatCareerDate"
    - from: "src/pages/about.astro"
      to: "about.markdown"
      via: "condensed bio content adaptation"
      pattern: "software engineer"
---

<objective>
Deliver hiring-grade Career and About pages with quantified timeline content and an expanded bio adapted from legacy about.markdown.

Purpose: Satisfy HIRE-05 (real employers, dates, bullets) and partial DESN-05 about content while staying inside Phase 3 scope.
Output: CareerTimeline component, date formatter, upgraded career.astro and about.astro.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hiring-pages/03-CONTEXT.md
@src/data/career.yml
@about.markdown
@src/pages/career.astro
@src/pages/about.astro
@.planning/phases/03-hiring-pages/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Career date formatter and vertical timeline</name>
  <files>src/lib/formatCareerDate.ts, src/lib/formatCareerDate.test.ts, src/components/CareerTimeline.astro, src/pages/career.astro</files>
  <behavior>
    - Test 1: formatCareerDate("2025-01") returns "Jan 2025"
    - Test 2: formatCareerDate("Present") returns "Present"
    - Test 3: formatCareerRange("2021-01", "2024-07") returns "Jan 2021 – Jul 2024"
  </behavior>
  <action>
    Create src/lib/formatCareerDate.ts exporting formatCareerDate(isoMonth: string) and formatCareerRange(start: string, end: string) per D-56. Parse YYYY-MM to short month + year; pass through "Present" unchanged; use en dash between range.

    Add minimal Node test file src/lib/formatCareerDate.test.ts using node:test + node:assert (no new test framework). Register npm script test:unit if missing.

    Build CareerTimeline.astro loading career entry roles and education. Layout: vertical timeline with left date rail and right content column per D-53. For each role show employer, title, formatted date range, and full bullets list (all items, no expand/collapse) per D-54 and D-07. Render education block below roles with institution and degree per D-55 and D-09.

    Wire into career.astro; remove @ts-nocheck and placeholder ul markup.
  </action>
  <verify>
    <automated>node --test src/lib/formatCareerDate.test.ts && npm run build && grep -q 'Invisible Technologies' dist/career/index.html && grep -q 'Designed a platform-wide maturity audit' dist/career/index.html && grep -q 'University of Washington' dist/career/index.html && grep -q 'Jan 2025' dist/career/index.html</automated>
  </verify>
  <done>Career page shows vertical timeline with formatted dates, all bullets per role, and education section below.</done>
</task>

<task type="auto">
  <name>Task 2: Expand About page bio from legacy content</name>
  <files>src/pages/about.astro</files>
  <action>
    Adapt about.markdown into 2-3 paragraphs for about.astro per D-60. Preserve voice covering backend/platform work, technical writing, and continuous learning — condense "What I Do", "My Journey", and "Beyond Code" sections; drop Jekyll meta footer and "This blog is built with Jekyll" closing.

    Required Connect section below bio with h2 "Let's Connect" (or equivalent) listing GitHub, LinkedIn, and mailto email links loaded from social.yml per D-60 — matching legacy about.markdown social block. This is required page body content in addition to header/footer chrome.

    Use semantic headings (h1 About, h2 for Connect). Load name from social.yml. Keep readable width within MainLayout main container.

    Do not add Person JSON-LD schema — Phase 6 scope.
  </action>
  <verify>
    <automated>npm run build && wc -w dist/about/index.html | awk '{ if ($1 >= 80) exit 0; exit 1 }' && grep -q 'technical writing\|software engineer' dist/about/index.html && grep -q 'Connect' dist/about/index.html && grep -q 'github.com/tazzledazzle' dist/about/index.html && grep -q 'linkedin.com/in/terenceschumacher' dist/about/index.html && grep -q 'mailto:terenceschumacher@gmail.com' dist/about/index.html</automated>
  </verify>
  <done>About page presents 2-3 paragraphs of hiring-focused bio plus required Connect section with GitHub, LinkedIn, and email links per D-60.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public career data | Employer names and achievements rendered statically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | career.yml content | accept | Build-time validated YAML; maintainer edits via git |
| T-03-09 | Information disclosure | Career bullets | accept | Intentional public hiring content |
</threat_model>

<verification>
Career dist page contains employers, quantified bullets, formatted dates, and education; About dist page has multi-paragraph bio; unit tests pass for date formatter.
</verification>

<success_criteria>
Recruiter reading /career/ sees real employers, human-readable dates, and full quantified bullets; /about/ has expanded bio beyond Phase 2 placeholder.
</success_criteria>

<output>
Create `.planning/phases/03-hiring-pages/03-04-SUMMARY.md` when done
</output>
