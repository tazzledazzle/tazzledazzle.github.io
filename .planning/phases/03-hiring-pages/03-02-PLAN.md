---
phase: 03-hiring-pages
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/pages/index.astro
  - src/components/Hero.astro
  - src/components/ProjectTease.astro
autonomous: true
requirements: [HIRE-01, HIRE-02, HIRE-06]
must_haves:
  truths:
    - "Homepage hero shows role label Platform-focused Software Engineer above the fold on desktop (per D-41)"
    - "Hero specialty line covers infrastructure/observability and platform tooling/DX (per D-42)"
    - "Hero uses value-proposition impact framing, not metric hook or current-employer lead (per D-43)"
    - "Hero CTA row offers Download Resume and GitHub; email/LinkedIn only via chrome (per D-48)"
    - "Below hero on desktop, 2-3 featured projects tease as proof-of-build (per D-44)"
    - "Resume download is reachable from hero CTA in addition to header (per D-46, HIRE-02, HIRE-06)"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Above-fold hiring pitch with CTAs"
    - path: "src/components/ProjectTease.astro"
      provides: "Compact featured-project preview row"
    - path: "src/pages/index.astro"
      provides: "Homepage assembling hero + tease"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/Hero.astro"
      via: "component import"
      pattern: "Hero"
    - from: "src/components/Hero.astro"
      to: "/resume.pdf"
      via: "Download Resume CTA"
      pattern: "Download Resume"
    - from: "src/components/ProjectTease.astro"
      to: "src/data/projects/featured.yml"
      via: "getCollection featuredProjects slice"
      pattern: "featuredProjects"
    - from: "src/components/ProjectTease.astro"
      to: "src/components/ProjectCard.astro"
      via: "compact card variant or reuse"
      pattern: "ProjectCard"
---

<objective>
Replace the Phase 2 homepage placeholder with a recruiter-optimized hero and featured-project tease that delivers the 60-second pitch above the fold.

Purpose: Satisfy HIRE-01 (role/specialty/impact framing) and HIRE-02 (prominent resume CTA on desktop without scrolling).
Output: Hero.astro, ProjectTease.astro, and upgraded index.astro.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hiring-pages/03-CONTEXT.md
@src/data/social.yml
@src/data/projects/featured.yml
@src/pages/index.astro
@src/components/ProjectCard.astro
@.planning/phases/03-hiring-pages/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero with role, specialty, value prop, and CTAs</name>
  <files>src/components/Hero.astro, src/pages/index.astro</files>
  <action>
    Create Hero.astro per D-41 through D-43 and D-48. Headline role label must read exactly "Platform-focused Software Engineer". Specialty subline must mention both infrastructure/observability AND platform tooling/DX in one sentence. Impact line uses value-proposition tone (e.g., building reliable platforms teams ship on) — no vanity metrics, no "currently at {employer}" opener.

    CTA row: primary button/link "Download Resume" → /resume.pdf and GitHub link from social.yml. Do not add email or LinkedIn buttons in hero — those remain in header/footer per D-48.

    Load name from social.yml for optional greeting. Use scoped styles to keep hero content above typical desktop fold (~900px viewport) without requiring scroll for headline + CTAs. Wire Hero into index.astro via MainLayout; remove Phase 2 placeholder paragraph and manual nav links.
  </action>
  <verify>
    <automated>npm run build && grep -q 'Platform-focused Software Engineer' dist/index.html && grep -qi 'observability\|infrastructure' dist/index.html && grep -qi 'platform\|tooling\|DX' dist/index.html && grep -q 'Download Resume' dist/index.html && grep -q 'href="/resume.pdf"' dist/index.html && grep -q 'github.com/tazzledazzle' dist/index.html</automated>
  </verify>
  <done>Homepage hero displays locked role label, dual-domain specialty line, value-prop impact framing, and Resume + GitHub CTAs only.</done>
</task>

<task type="auto">
  <name>Task 2: Add featured project tease below hero</name>
  <files>src/components/ProjectTease.astro, src/pages/index.astro</files>
  <action>
    Create ProjectTease.astro loading getCollection('featuredProjects') and rendering 2-3 projects (planner discretion: prefer highest-signal mix of platform/observability/tooling) per D-44. Reuse ProjectCard from 03-01 or a compact variant with title, summary snippet, tech pills, and GitHub link — include "View all work" link to /work/.

    Place tease immediately below hero on index.astro so desktop users see proof-of-build before fold ends per D-44. Keep layout responsive: stack tease cards single-column on mobile.

    Ensure homepage no longer duplicates header navigation or social links inline.
  </action>
  <verify>
    <automated>npm run build && grep -c 'Native macOS Log Analyzer\|OpenTelemetry Demo Stack\|WebSocket Chat\|ImgAnnotator\|Online Bookstore' dist/index.html | awk '{ if ($1 >= 2) exit 0; exit 1 }' && grep -q 'href="/work/"' dist/index.html</automated>
  </verify>
  <done>Homepage shows 2-3 featured project teases with path to /work/ below the hero on desktop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time content | Hero copy derived from locked decisions + social.yml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Hero external links | mitigate | GitHub URL sourced from validated social.yml at build time |
| T-03-05 | Repudiation | Static hero copy | accept | Git-versioned Astro components; no runtime mutation |
</threat_model>

<verification>
dist/index.html contains role label, resume CTA, GitHub CTA, and at least one featured project title with /work/ link.
</verification>

<success_criteria>
Recruiter landing on / sees role, specialty, impact framing, resume/GitHub CTAs, and featured project proof without scrolling on desktop.
</success_criteria>

<output>
Create `.planning/phases/03-hiring-pages/03-02-SUMMARY.md` when done
</output>
