---
phase: 03-hiring-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/MainLayout.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/MobileNav.astro
  - src/components/ProjectCard.astro
  - src/styles/global.css
  - public/resume.pdf
autonomous: false
requirements: [HIRE-06, HIRE-07]
user_setup:
  - service: resume-pdf
    why: "HIRE-06 requires a downloadable resume at public/resume.pdf (D-45)"
    dashboard_config:
      - task: "Export current resume as PDF and place at public/resume.pdf before final verification"
        location: "Local filesystem — repo public/ directory"
must_haves:
  truths:
    - "Every page renders data-driven primary navigation from navigation.yml (per D-57)"
    - "GitHub, LinkedIn, and email appear in both header and footer on every page (per D-58)"
    - "Resume download link labeled Download Resume appears in the site header (per D-46, D-47)"
    - "Mobile viewport under 768px shows hamburger navigation instead of inline nav links (per D-59)"
    - "public/resume.pdf exists and header links to /resume.pdf (per D-45)"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Data-driven nav, social links, resume CTA, mobile toggle"
    - path: "src/components/Footer.astro"
      provides: "Social links and site footer chrome"
    - path: "src/components/MobileNav.astro"
      provides: "Collapsible mobile navigation panel"
    - path: "src/components/ProjectCard.astro"
      provides: "Reusable project card with tech pills and conditional demo link"
    - path: "src/layouts/MainLayout.astro"
      provides: "Shared layout wrapping header, main slot, footer"
    - path: "public/resume.pdf"
      provides: "Static resume PDF for one-click download"
  key_links:
    - from: "src/components/Header.astro"
      to: "src/data/navigation.yml"
      via: "getCollection('navigation') sorted by order"
      pattern: "getCollection\\(['\"]navigation"
    - from: "src/components/Header.astro"
      to: "src/data/social.yml"
      via: "getEntry('social', 'default')"
      pattern: "getEntry\\(['\"]social"
    - from: "src/components/Header.astro"
      to: "/resume.pdf"
      via: "anchor href"
      pattern: "Download Resume"
    - from: "src/components/ProjectCard.astro"
      to: "project.github_url"
      via: "primary GitHub action link"
      pattern: "github_url"
---

<objective>
Establish shared site chrome and the reusable ProjectCard component so all hiring pages share navigation, social access, and resume download.

Purpose: Unblock HIRE-06 (header resume) and HIRE-07 (persistent social/contact chrome) before page-specific layouts ship.
Output: Header, Footer, MobileNav, ProjectCard, MainLayout integration, and public/resume.pdf.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hiring-pages/03-CONTEXT.md
@src/data/navigation.yml
@src/data/social.yml
@src/data/projects/featured.yml
@src/content.config.ts
@src/layouts/MainLayout.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Header, Footer, MobileNav, and wire MainLayout</name>
  <files>src/components/Header.astro, src/components/Footer.astro, src/components/MobileNav.astro, src/layouts/MainLayout.astro, src/styles/global.css</files>
  <action>
    Create src/components/Header.astro loading navigation via getCollection('navigation') sorted by order field and social via getEntry('social', 'default') per D-57 and D-58. Render primary nav links from YAML (About, Work, Career, Blog). Add social icons or text links for GitHub, LinkedIn, and mailto email in header. Add resume anchor with exact label "Download Resume" pointing to /resume.pdf per D-46 and D-47.

    Create Footer.astro repeating the same three social/contact links (GitHub, LinkedIn, mailto email) per D-58 — do not duplicate full primary nav in footer unless it aids scanability; social/contact parity is the requirement. Add class site-footer on the footer root element for verification scoping.

    Implement MobileNav.astro using native details/summary or equivalent zero-JS collapse for viewports under 768px per D-59 and Claude discretion — avoid React islands. Hide inline nav links below md breakpoint; show hamburger control that expands nav + resume link. Ensure keyboard operability (summary focusable, links reachable).

    Update MainLayout.astro to import Header and Footer, wrap slot in semantic main, preserve title/description props. Extend global.css minimally for header/footer layout (flex, spacing, border-bottom) within existing CSS variable tokens — do not introduce Phase 5 design-system tokens.

    Remove inline nav links from any page stubs that duplicate header nav once MainLayout ships.
  </action>
  <verify>
    <automated>npm run build && test -f dist/index.html && grep -q 'Download Resume' dist/index.html && grep -q 'href="/resume.pdf"' dist/index.html && grep -q 'github.com/tazzledazzle' dist/index.html && grep -q 'linkedin.com/in/terenceschumacher' dist/index.html && grep -q 'mailto:terenceschumacher@gmail.com' dist/index.html && grep -A20 'site-footer' dist/index.html | grep -q 'github.com/tazzledazzle' && grep -A20 'site-footer' dist/index.html | grep -q 'linkedin.com/in/terenceschumacher' && grep -qE 'details|aria-label=.Menu|mobile-nav' dist/index.html</automated>
  </verify>
  <done>Built site renders header/footer on every page with YAML-driven nav, social links in both chrome regions, and labeled resume download in header.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provide resume PDF at public/resume.pdf</name>
  <files>public/resume.pdf</files>
  <action>
    Export your current hiring resume as PDF and save it to public/resume.pdf in the repo (D-45). If a placeholder already exists from Task 1 scaffolding, replace it with your real resume before approving this checkpoint.
  </action>
  <verify>
    <automated>test -f public/resume.pdf && test -s public/resume.pdf</automated>
  </verify>
  <done>public/resume.pdf is present, non-empty, and ready for one-click download in production.</done>
  <resume-signal>Type "done" after placing resume.pdf, or describe if you need a temporary placeholder to proceed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create shared ProjectCard component</name>
  <files>src/components/ProjectCard.astro</files>
  <action>
    Create ProjectCard.astro accepting a featuredProjects or archiveProjects collection entry. Render title, summary (impact framing), tech_stack as pill/chip tags per D-50, and a primary GitHub action linking to github_url per D-51. Render demo link only when demo_url is non-null; when demo_status is code_only and demo_url is null, omit demo UI entirely — no badge, no "code only" label per D-51 (overrides ROADMAP criterion 4 wording). Use scoped component styles for card grid item layout; keep Tailwind utility classes consistent with Phase 2 minimal shell.

    Export a typed Props interface using the project schema shape from content.config.ts (id, title, summary, tech_stack, github_url, demo_url, demo_status).
  </action>
  <verify>
    <automated>npm run build && npx astro check</automated>
  </verify>
  <done>ProjectCard renders featured project data with tech pills, GitHub primary action, and conditional demo link behavior matching D-50 and D-51.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static PDF in public/ | User-downloadable file served as-is from GitHub Pages |
| External social URLs | Outbound links to GitHub, LinkedIn, mailto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | public/resume.pdf | accept | Maintainer-controlled static asset; no server processing |
| T-03-02 | Spoofing | mailto/social links | mitigate | Load URLs from validated social.yml via Zod schema at build time |
| T-03-03 | Information disclosure | resume.pdf | accept | Intentional public hiring artifact per D-45 |
| T-03-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
npm run build succeeds; dist/index.html contains Download Resume, /resume.pdf, GitHub, LinkedIn, and mailto links; public/resume.pdf exists after checkpoint.
</verification>

<success_criteria>
Header/footer chrome is live on all pages; resume PDF downloads from header; ProjectCard component exists for downstream pages.
</success_criteria>

<output>
Create `.planning/phases/03-hiring-pages/03-01-SUMMARY.md` when done
</output>
