---
phase: 02-astro-scaffold-content-migration
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/content.config.ts
  - src/data/navigation.yml
  - src/data/social.yml
  - src/data/career.yml
  - src/data/projects/featured.yml
  - src/data/projects/archive.yml
  - src/data/blog-inventory.yml
  - lib/tasks/data.rake
autonomous: true
requirements: [PLAT-01]
must_haves:
  truths:
    - "Phase 1 data is available to Astro from `src/data` with typed collection loaders (per D-36)."
    - "Permalink and canonical metadata source of truth remains `blog-inventory.yml` (per D-26)."
    - "Ruby validation command `rake data:validate` remains operational after migration changes (per D-37)."
  artifacts:
    - path: "src/content.config.ts"
      provides: "Astro content/data collection contracts for migrated files"
    - path: "src/data/blog-inventory.yml"
      provides: "Route and redirect authority metadata for PLAT-02/PLAT-05"
    - path: "lib/tasks/data.rake"
      provides: "Validator continuity after _data -> src/data migration"
  key_links:
    - from: "src/content.config.ts"
      to: "src/data/*.yml"
      via: "file() loaders + schema validation"
      pattern: "file\\("
    - from: "lib/tasks/data.rake"
      to: "scripts/validate-data.rb"
      via: "rake task invocation"
      pattern: "ruby scripts/validate-data\\.rb"
    - from: "src/data/blog-inventory.yml"
      to: "future blog routes and redirect generator"
      via: "permalink/canonical mapping"
      pattern: "canonical_slug"
---

<objective>
Migrate the structured data layer into Astro collections and preserve validation continuity before blog route and feed work.

Purpose: Complete the D-36/D-37 platform data migration preconditions for downstream PLAT-02 through PLAT-05 implementation.
Output: `src/data` migration, typed collection wiring, and retained `rake data:validate` command behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-astro-scaffold-content-migration/02-CONTEXT.md
@.planning/phases/02-astro-scaffold-content-migration/02-RESEARCH.md
@docs/ASTRO-DATA-MAPPING.md
@.planning/phases/02-astro-scaffold-content-migration/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy Phase 1 data to Astro data directory</name>
  <files>src/data/navigation.yml, src/data/social.yml, src/data/career.yml, src/data/projects/featured.yml, src/data/projects/archive.yml, src/data/blog-inventory.yml</files>
  <behavior>
    - Test 1: `src/data` contains complete copies of required Phase 1 source YAML files.
    - Test 2: `blog-inventory.yml` in `src/data` preserves permalink and canonical fields used later by D-25/D-30 logic.
  </behavior>
  <action>Copy Phase 1 `_data` content into `src/data` exactly per D-36, with no schema-altering transformations. Preserve `blog-inventory.yml` fields unchanged so downstream permalink and redirect plans implement D-25, D-26, and D-30 from the same source.</action>
  <verify>
    <automated>bundle exec rake data:validate</automated>
  </verify>
  <done>All required data files exist in `src/data` and pass validator checks without content loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define typed Astro loaders for migrated data collections</name>
  <files>src/content.config.ts</files>
  <behavior>
    - Test 1: Astro can load each migrated data file via collection/file loader without type errors.
    - Test 2: Inventory collection exposes permalink/canonical fields required by D-26 downstream consumers.
  </behavior>
  <action>Implement `src/content.config.ts` schemas and file loaders for migrated YAML files so all data is queryable in Astro, explicitly preserving inventory semantics per D-26. Keep schema strict enough to fail on malformed entries instead of silently coercing content.</action>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>Astro data collections type-check and all migrated YAML files resolve through the configured loaders.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve validator command behavior after path migration</name>
  <files>lib/tasks/data.rake</files>
  <action>Adjust rake task path references only as needed to keep `rake data:validate` operational after `_data` migration, per D-37. Do not remove Ruby validation tooling and do not rewrite the validator to a new runtime in this phase.</action>
  <verify>
    <automated>bundle exec rake data:validate && npx astro check</automated>
  </verify>
  <done>Validation command remains green and continues to guard migrated data quality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown frontmatter -> content collections | Legacy post metadata enters typed parsing boundary |
| inventory manifest -> route generation | Route authority comes from YAML mapping source |
| build output XML -> public crawlers/feed readers | Discovery artifacts become search/feed surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | migrated YAML data files | mitigate | Use strict loader schemas and fail-fast validation on malformed data |
| T-02-05 | Tampering | `blog-inventory.yml` authority data | mitigate | Keep inventory copied verbatim and treat it as canonical mapping source |
| T-02-06 | Denial of Service | data validation workflow | mitigate | Preserve `rake data:validate` path correctness through migration |
| T-02-SC | Tampering | npm installs | mitigate | Install only legitimacy-audited packages from research artifact |
</threat_model>

<verification>
- `bundle exec rake data:validate`
- `npx astro check`
- `npm run build`
</verification>

<success_criteria>
- Data migration to `src/data` is complete and typed (D-36).
- Inventory remains authoritative for downstream routing and redirects (D-26).
- Validator continuity is preserved for remaining phases (D-37).
</success_criteria>

<output>
Create `.planning/phases/02-astro-scaffold-content-migration/02-02-SUMMARY.md` when done.
</output>
