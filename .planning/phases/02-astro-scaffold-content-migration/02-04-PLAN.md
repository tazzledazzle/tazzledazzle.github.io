---
phase: 02-astro-scaffold-content-migration
plan: 04
type: execute
wave: 4
depends_on: [02-02, 02-03]
files_modified:
  - src/pages/rss.xml.ts
  - astro.config.mjs
  - scripts/verify-phase2-routes.mjs
  - scripts/generate-legacy-redirects.mjs
  - public/projects/index.html
  - public/About/index.html
  - public/Projects/index.html
  - public/Career/index.html
autonomous: true
requirements: [PLAT-03, PLAT-04, PLAT-05]
must_haves:
  truths:
    - "RSS includes only featured/standard posts and excludes archived posts (per D-32)."
    - "Sitemap excludes archived posts while archived URLs still build and resolve (per D-33, D-16)."
    - "Legacy `/projects/` and mixed-case nav aliases redirect to canonical lowercase targets (per D-28, D-29, D-31)."
    - "Duplicate legacy URLs redirect to canonical slugs using inventory metadata (per D-30, D-17, D-26)."
  artifacts:
    - path: "src/pages/rss.xml.ts"
      provides: "RSS endpoint with archived-tier exclusions"
    - path: "scripts/generate-legacy-redirects.mjs"
      provides: "Deterministic redirect generation from inventory and alias map"
    - path: "scripts/verify-phase2-routes.mjs"
      provides: "Automated checks for RSS/sitemap policy and redirect coverage"
  key_links:
    - from: "src/pages/rss.xml.ts"
      to: "src/data/blog-inventory.yml"
      via: "tier-based filtering for feed items"
      pattern: "tier !== 'archived'"
    - from: "astro.config.mjs"
      to: "sitemap integration"
      via: "filter archived URLs from sitemap output"
      pattern: "sitemap"
    - from: "scripts/generate-legacy-redirects.mjs"
      to: "public/* redirect files"
      via: "meta-refresh generation"
      pattern: "canonical_slug"
---

<objective>
Implement discovery artifact filtering and redirect compatibility so legacy links, feed behavior, and crawlability policies are all enforced.

Purpose: Deliver PLAT-03, PLAT-04, and PLAT-05 policies tied to archived handling and URL consolidation decisions.
Output: RSS + sitemap filtering, redirect generator/output files, and automated policy checks.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-astro-scaffold-content-migration/02-CONTEXT.md
@.planning/phases/02-astro-scaffold-content-migration/02-RESEARCH.md
@src/data/blog-inventory.yml
@.planning/phases/02-astro-scaffold-content-migration/02-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate RSS and sitemap with archived exclusion policy</name>
  <files>src/pages/rss.xml.ts, astro.config.mjs, scripts/verify-phase2-routes.mjs</files>
  <behavior>
    - Test 1: RSS output contains no archived permalinks and includes featured/standard posts.
    - Test 2: Sitemap excludes archived permalinks while preserving canonical hiring/blog routes.
  </behavior>
  <action>Implement feed/sitemap generation filters to enforce D-32 and D-33 using inventory tier data. Keep archived posts buildable/routable per D-16 while excluding them from discovery surfaces.</action>
  <verify>
    <automated>npm run build && node scripts/verify-phase2-routes.mjs --check rss-sitemap</automated>
  </verify>
  <done>RSS and sitemap outputs match archive policy and pass automated parity checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate legacy redirect set from alias and canonical maps</name>
  <files>scripts/generate-legacy-redirects.mjs, public/projects/index.html, public/About/index.html, public/Projects/index.html, public/Career/index.html</files>
  <behavior>
    - Test 1: `/projects/` redirects to `/work/`.
    - Test 2: Mixed-case nav aliases redirect to lowercase canonical routes.
    - Test 3: Duplicate 2015 legacy links redirect to canonical_slug destinations.
  </behavior>
  <action>Create GitHub Pages-compatible meta-refresh redirects per D-28 using explicit nav aliases (D-29, D-31) and inventory duplicate mappings (D-30, D-17, D-26). Ensure generated redirects contain both meta refresh and canonical link targets.</action>
  <verify>
    <automated>node scripts/generate-legacy-redirects.mjs && node scripts/verify-phase2-routes.mjs --check redirects</automated>
  </verify>
  <done>Required redirect set is generated deterministically with correct canonical destinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory metadata -> redirect targets | Canonical mapping data controls redirect destination |
| feed/sitemap generators -> search/feed consumers | Generated XML affects discovery exposure |
| static redirect files -> visitor navigation | Public URL access depends on generated redirect correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information Disclosure | RSS and sitemap | mitigate | Filter archived tier in both outputs and assert exclusions in verification script |
| T-02-11 | Spoofing | redirect targets | mitigate | Build targets from allowlisted aliases and inventory canonical_slug only |
| T-02-12 | Tampering | redirect generation scripts | mitigate | Keep deterministic input sources and enforce scripted checks in CI |
| T-02-SC | Tampering | npm installs | mitigate | Use audited packages from research legitimacy table |
</threat_model>

<verification>
- `npm run build`
- `node scripts/verify-phase2-routes.mjs --check rss-sitemap`
- `node scripts/generate-legacy-redirects.mjs`
- `node scripts/verify-phase2-routes.mjs --check redirects`
</verification>

<success_criteria>
- PLAT-03 and PLAT-04 discovery artifacts enforce archived exclusion (D-32, D-33).
- PLAT-05 redirect coverage is implemented for changed and consolidated legacy URLs (D-28 through D-31).
</success_criteria>

<output>
Create `.planning/phases/02-astro-scaffold-content-migration/02-04-SUMMARY.md` when done.
</output>
