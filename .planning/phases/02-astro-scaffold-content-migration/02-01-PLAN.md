---
phase: 02-astro-scaffold-content-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - .github/workflows/deploy-pages.yml
  - src/styles/global.css
autonomous: true
requirements: [PLAT-01]
must_haves:
  truths:
    - "Main deploy path switches to Astro in one cutover with no dual Jekyll/Astro CI (per D-21, D-22)."
    - "Astro static output builds for GitHub Pages and supports a clean rollback by reverting the cutover commit (per D-24)."
    - "Minimal Tailwind 4 shell tokens exist for readable baseline styling, not full design-system polish (per D-38)."
  artifacts:
    - path: ".github/workflows/deploy-pages.yml"
      provides: "Astro build and deploy workflow for GitHub Pages"
    - path: "astro.config.mjs"
      provides: "Astro site configuration for tazzledazzle.github.io static output"
    - path: "package.json"
      provides: "Astro 7 + Tailwind 4 dependency and script baseline"
  key_links:
    - from: ".github/workflows/deploy-pages.yml"
      to: "astro build artifact deployment"
      via: "withastro/action@v6 -> actions/deploy-pages@v5"
      pattern: "withastro/action@v6"
    - from: "package.json"
      to: "astro.config.mjs"
      via: "dependency/runtime compatibility"
      pattern: "astro"
    - from: "src/styles/global.css"
      to: "Astro page shell"
      via: "global style import"
      pattern: "@import"
---

<objective>
Stand up Astro 7 as the active deployment platform and lock in the big-bang CI cutover baseline for Phase 2 migration work.

Purpose: Satisfy the PLAT-01 platform transition constraints before content, routing, and redirect migration tasks begin.
Output: Astro scaffold, deploy workflow replacement, baseline global styling tokens, and rollback-safe cutover structure.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-astro-scaffold-content-migration/02-CONTEXT.md
@.planning/phases/02-astro-scaffold-content-migration/02-RESEARCH.md
@docs/ASTRO-DATA-MAPPING.md
@.planning/phases/01-foundation-data-extraction/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro scaffold and production deploy workflow</name>
  <files>package.json, astro.config.mjs, tsconfig.json, .github/workflows/deploy-pages.yml</files>
  <action>Initialize Astro 7 static scaffolding and configure the GitHub Pages workflow as the single production path per D-21 and D-22. Set `site` for `tazzledazzle.github.io` and keep deployment changes grouped so D-24 rollback is a single `git revert` operation.</action>
  <verify>
    <automated>npm install && npm run build</automated>
  </verify>
  <done>Astro build succeeds and CI config points to an Astro-only deploy pipeline on main.</done>
</task>

<task type="auto">
  <name>Task 2: Apply minimal Tailwind shell baseline</name>
  <files>src/styles/global.css</files>
  <action>Add only the minimal dark-theme typography and spacing baseline required for readable Phase 2 pages per D-38. Do not introduce full design-system tokens, component variants, or polish that belongs to deferred Phase 5.</action>
  <verify>
    <automated>npm run build && npx astro check</automated>
  </verify>
  <done>Global styles provide a readable minimum shell and remain intentionally scoped to Phase 2 minimal presentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo content -> Astro content loader | Untrusted markdown/yaml metadata is parsed at build time |
| CI build pipeline -> production Pages artifact | Build output becomes public site content |
| Ruby validator task -> migrated data paths | Validation command must read correct sources after path changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | workflow and package baseline | mitigate | Keep official Astro workflow/action pair and lock config to audited package set |
| T-02-02 | Repudiation | rollback procedure | mitigate | Keep cutover in explicit commit boundary to support D-24 revert path |
| T-02-03 | Denial of Service | CI deploy workflow | mitigate | Keep workflow minimal and deterministic with single Astro build path |
| T-02-SC | Tampering | npm installs | mitigate | Use audited packages from 02-RESEARCH legitimacy table only |
</threat_model>

<verification>
- `npm run build` succeeds from clean install.
- `npx astro check` passes for scaffold configuration.
</verification>

<success_criteria>
- PLAT-01 cutover mechanics are established with a single Astro workflow path (D-21, D-22).
- Rollback remains operational via straightforward revert procedure (D-24).
- Minimal style shell exists and remains in-scope for Phase 2 (D-38).
</success_criteria>

<output>
Create `.planning/phases/02-astro-scaffold-content-migration/02-01-SUMMARY.md` when done.
</output>
