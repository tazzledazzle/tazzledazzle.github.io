---
phase: 02-astro-scaffold-content-migration
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/content/blog/*
  - src/pages/blog/[...slug].astro
  - src/pages/blog/index.astro
  - src/layouts/MainLayout.astro
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/work.astro
  - src/pages/career.astro
autonomous: true
requirements: [PLAT-02]
must_haves:
  truths:
    - "All migrated blog posts are available in Astro content and routable at legacy Jekyll permalink format (per D-25, D-35)."
    - "Permalink resolution uses `blog-inventory.yml` as source of truth, not filename-derived slug inference (per D-26)."
    - "Canonical static pages are present at `/about/`, `/work/`, `/career/`, and `/blog/` and `/work/` remains canonical projects URL (per D-27, D-05)."
    - "Phase 2 ships basic hiring pages and readable minimal blog rendering with syntax highlighting (per D-39, D-40)."
  artifacts:
    - path: "src/content/blog/"
      provides: "Migrated post corpus"
    - path: "src/pages/blog/[...slug].astro"
      provides: "Inventory-driven route resolution for legacy URL parity"
    - path: "src/pages/work.astro"
      provides: "Basic hiring work page wired to migrated project data"
  key_links:
    - from: "src/pages/blog/[...slug].astro"
      to: "src/data/blog-inventory.yml"
      via: "permalink lookup and route params build"
      pattern: "blog-inventory"
    - from: "src/pages/blog/index.astro"
      to: "src/content/blog/*"
      via: "collection query + list rendering"
      pattern: "getCollection\\('blog'\\)"
    - from: "src/pages/work.astro"
      to: "src/data/projects/featured.yml"
      via: "projects data render"
      pattern: "featured"
---

<objective>
Implement blog and canonical page rendering with inventory-accurate routing so migrated content is browsable at preserved URLs.

Purpose: Deliver PLAT-02 and the D-27/D-39/D-40 page scope on top of the migrated Astro data layer.
Output: Migrated posts, permalink route implementation, readable blog UI, and basic hiring pages.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-astro-scaffold-content-migration/02-CONTEXT.md
@.planning/phases/02-astro-scaffold-content-migration/02-RESEARCH.md
@docs/ASTRO-DATA-MAPPING.md
@src/data/blog-inventory.yml
@.planning/phases/02-astro-scaffold-content-migration/02-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate legacy posts into Astro content</name>
  <files>src/content/blog/*</files>
  <behavior>
    - Test 1: Migrated post count matches inventory post count.
    - Test 2: Frontmatter normalization preserves title/content while mapping legacy fields for Astro.
  </behavior>
  <action>Move all `_posts` content to `src/content/blog` per D-35 and normalize frontmatter (date to pubDate, categories to tags, drop Jekyll-only layout) while preserving body fidelity. Keep permalink authority in inventory per D-26 rather than deriving route IDs from filenames.</action>
  <verify>
    <automated>npx astro check && npm run build</automated>
  </verify>
  <done>All legacy posts exist in Astro content with valid metadata for routing and rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build inventory-driven blog routes and readable post/index pages</name>
  <files>src/pages/blog/[...slug].astro, src/pages/blog/index.astro, src/layouts/MainLayout.astro</files>
  <behavior>
    - Test 1: Generated static paths match legacy permalink format `/:year/:month/:day/:title/`.
    - Test 2: Archived posts remain routable by direct URL while excluded from primary index listing.
    - Test 3: Post body code blocks render with syntax highlighting.
  </behavior>
  <action>Implement `getStaticPaths()` from `blog-inventory.yml` permalink values exactly per D-25 and D-26, then render a minimal readable blog page/index per D-40. Keep presentation intentionally simple and avoid deferred discovery features (tags/year archives/prev-next are Phase 4 deferred items).</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Legacy blog permalinks resolve correctly and blog pages are readable with syntax-highlighted content.</done>
</task>

<task type="auto">
  <name>Task 3: Add basic canonical hiring routes</name>
  <files>src/pages/index.astro, src/pages/about.astro, src/pages/work.astro, src/pages/career.astro</files>
  <action>Implement the basic hiring page scope now per D-39 with canonical static routes from D-27 (`/about/`, `/work/`, `/career/`, `/blog/`) and keep `/work/` as canonical projects route per D-05. Ensure these pages consume migrated data where applicable and avoid deep case-study expansion deferred beyond this phase.</action>
  <verify>
    <automated>npm run build && npx astro check</automated>
  </verify>
  <done>Canonical hiring routes are present and functional with basic data-backed content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy URL requests -> redirect pages | User traffic hits generated static redirect surfaces |
| inventory canonical data -> redirect targets | Canonical data controls redirection destination |
| cleanup step -> production availability | Deleting legacy files can break rollback if done prematurely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | post migration mapping | mitigate | Preserve inventory-driven permalink mapping and verify route generation parity |
| T-02-08 | Information Disclosure | archived post visibility | mitigate | Keep archived entries out of primary index while preserving direct URL behavior |
| T-02-09 | Spoofing | canonical page routes | mitigate | Enforce D-27 lowercase canonical paths and D-05 `/work/` path authority |
| T-02-SC | Tampering | npm installs | mitigate | Continue using legitimacy-audited package set; no unvetted additions |
</threat_model>

<verification>
- `npm run build`
- `npx astro check`
</verification>

<success_criteria>
- PLAT-02 permalink parity is implemented from inventory data (D-25, D-26).
- Basic hiring and blog pages are shipped in Phase 2 scope (D-27, D-39, D-40).
- Post corpus is migrated from Jekyll source to Astro content (D-35).
</success_criteria>

<output>
Create `.planning/phases/02-astro-scaffold-content-migration/02-03-SUMMARY.md` when done.
</output>
