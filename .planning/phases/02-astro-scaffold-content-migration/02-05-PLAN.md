---
phase: 02-astro-scaffold-content-migration
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - scripts/verify-phase2-routes.mjs
  - .planning/phases/02-astro-scaffold-content-migration/02-VALIDATION.md
  - Gemfile
  - Gemfile.lock
  - _config.yml
  - _layouts/*
  - _posts/*
  - _data/*
autonomous: true
requirements: [PLAT-01]
must_haves:
  truths:
    - "Pre-merge validation enforces a fixed 10-link permalink smoke suite plus RSS/sitemap checks (per D-23)."
    - "Rollback procedure remains explicit and executable as a git revert to pre-cutover Jekyll commit (per D-24)."
    - "Jekyll runtime artifacts are retired only after Astro migration gates pass (per D-34, D-35, D-36)."
    - "Ruby validation tooling remains available after retirement cleanup (per D-37)."
  artifacts:
    - path: ".planning/phases/02-astro-scaffold-content-migration/02-VALIDATION.md"
      provides: "Locked validation matrix including deterministic smoke URL set"
    - path: "scripts/verify-phase2-routes.mjs"
      provides: "D-23 validation enforcement in automated checks"
    - path: "_config.yml"
      provides: "Removed as active Jekyll runtime artifact post-cutover"
  key_links:
    - from: "02-VALIDATION.md"
      to: "scripts/verify-phase2-routes.mjs"
      via: "smoke matrix parity with script assertions"
      pattern: "smoke10"
    - from: "cleanup task"
      to: "rollback plan"
      via: "execute cleanup only after green validation gates"
      pattern: "git revert"
    - from: "lib/tasks/data.rake"
      to: "post-cleanup repo behavior"
      via: "validator continuity after Jekyll retirement"
      pattern: "data:validate"
---

<objective>
Finalize cutover safety gates, document deterministic validation, and retire legacy Jekyll runtime assets after migration checks pass.

Purpose: Complete PLAT-01 transition controls so production Astro cutover is verifiable and reversible.
Output: Validation matrix doc, smoke test enforcement updates, and scoped Jekyll retirement cleanup.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-astro-scaffold-content-migration/02-CONTEXT.md
@.planning/phases/02-astro-scaffold-content-migration/02-RESEARCH.md
@.planning/phases/02-astro-scaffold-content-migration/02-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock D-23 validation matrix and script parity</name>
  <files>scripts/verify-phase2-routes.mjs, .planning/phases/02-astro-scaffold-content-migration/02-VALIDATION.md</files>
  <behavior>
    - Test 1: Smoke validation checks exactly 10 predetermined legacy URLs across featured/standard/archived/duplicate categories.
    - Test 2: Validation suite includes RSS and sitemap policy checks in the same pre-merge gate.
  </behavior>
  <action>Implement and document a deterministic D-23 smoke matrix in `02-VALIDATION.md` and ensure script checks enforce the same set. Include clear pass/fail criteria and command sequence for pre-merge validation.</action>
  <verify>
    <automated>npm run build && node scripts/verify-phase2-routes.mjs --check smoke10</automated>
  </verify>
  <done>Validation doc and script use the same fixed smoke matrix and pass criteria.</done>
</task>

<task type="auto">
  <name>Task 2: Remove active Jekyll runtime files after green gates</name>
  <files>Gemfile, Gemfile.lock, _config.yml, _layouts/*, _posts/*, _data/*</files>
  <action>After validation gates are green, remove active Jekyll runtime assets per D-34, D-35, and D-36. Keep Ruby validation tooling and `rake data:validate` command behavior per D-37, and document D-24 rollback command in `02-VALIDATION.md`.</action>
  <verify>
    <automated>npm run build && bundle exec rake data:validate && rg "_layouts|_posts/|_data/" .planning/phases/02-astro-scaffold-content-migration/02-VALIDATION.md</automated>
  </verify>
  <done>Active Jekyll runtime footprint is retired and post-cutover validation/rollback procedures remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation script -> release decision | Validation outcome gates whether cleanup/cutover proceed |
| cleanup operations -> production site stack | Removing legacy files can break deployment if misordered |
| rollback instructions -> recovery path | Bad rollback guidance increases outage duration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Repudiation | pre-merge validation | mitigate | Keep deterministic scripted checks and explicit validation doc matrix |
| T-02-14 | Tampering | cleanup sequence | mitigate | Gate cleanup on passing smoke/build validation before deletion |
| T-02-15 | Denial of Service | rollback process | mitigate | Document and verify D-24 revert command path in validation artifact |
| T-02-SC | Tampering | npm installs | mitigate | Keep dependency set limited to audited packages |
</threat_model>

<verification>
- `npm run build`
- `node scripts/verify-phase2-routes.mjs --check smoke10`
- `bundle exec rake data:validate`
</verification>

<success_criteria>
- D-23 smoke checks are deterministic and documented.
- D-24 rollback remains actionable.
- D-34 through D-37 retirement constraints are met without validation regression.
</success_criteria>

<output>
Create `.planning/phases/02-astro-scaffold-content-migration/02-05-SUMMARY.md` when done.
</output>
