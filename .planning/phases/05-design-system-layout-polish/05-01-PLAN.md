---
phase: 05-design-system-layout-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
autonomous: true
requirements: [DESN-01, DESN-03]
must_haves:
  truths:
    - "Visitor sees GitHub Dark-inspired palette harmonized with Expressive Code on first load"
    - "Interactive elements share .btn and .card primitives with ~4px radius"
    - "Keyboard focus shows visible rings on links and buttons"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Extended CSS custom property token set and shared component classes"
      contains: "--bg"
    - path: "src/styles/global.css"
      provides: ".btn-primary shared CTA styling"
      contains: ".btn-primary"
    - path: "src/styles/global.css"
      provides: ".card surface token"
      contains: ".card"
  key_links:
    - from: "src/styles/global.css"
      to: ":root"
      via: "color-scheme dark and GitHub Dark token variables"
      pattern: "color-scheme:\\s*dark"
    - from: "src/styles/global.css"
      to: "Expressive Code github-dark"
      via: "aligned --bg/--fg/--border hues"
      pattern: "--border"
---

## Phase Goal

**As a** visitor, **I want** a cohesive, professional dark-theme experience across all pages and screen sizes, **so that** I can navigate, read comfortably, and trust the site's technical credibility.

<objective>
Establish GitHub Dark-inspired design tokens and shared `.btn` / `.card` primitives in global.css — foundation for all Phase 5 visual polish (D-85–D-89, D-97, D-98, D-104, DESN-01, DESN-03).

Purpose: Replace ad-hoc per-component colors with a unified token system aligned to Expressive Code github-dark (D-71).
Output: Extended `:root` tokens, `.btn`/`.btn-primary`, `.card`, and `:focus-visible` ring styles in `src/styles/global.css`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-design-system-layout-polish/05-CONTEXT.md
@src/styles/global.css
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Dark token palette and dark default</name>
  <files>src/styles/global.css</files>
  <action>
    Replace minimal `:root` block with GitHub Dark-inspired custom properties per D-85, D-86, D-88, D-89, DESN-03. Map at minimum: `--bg`, `--fg`, `--muted`, `--accent`, `--border`, `--surface` (card/elevated bg). Use hex values that harmonize with Expressive Code `github-dark` theme (D-71) — reference GitHub Primer dark palette (#0d1117 bg, #e6edf3 fg, #8b949e muted, #58a6ff accent, #30363d border, #161b22 surface). Keep `color-scheme: dark` on `:root`.

    Per D-86: reserve `--accent` for links, tags, and subtle highlights — do not flood large surfaces with accent.

    Per D-87: document in a brief comment that spacing uses Tailwind default scale (4/8/12/16/24px utilities) — no custom spacing tokens required.

    Per D-88: set shared `--radius: 0.25rem` (~4px) for buttons and cards.

    Per D-89: keep Inter in `font-family` stack (existing pattern); do not add `@tailwindcss/typography` (deferred per CONTEXT).

    Update existing rules that hardcode old token-derived mixes to use new `--border` / `--surface` where applicable. Do NOT change layout widths or `main` max-width yet (Plan 02). Do NOT remove global `p { color: muted }` yet (Plan 02 fixes D-92).
  </action>
  <verify>
    <automated>grep -E '(--bg|--fg|--accent|--border|--surface|--radius)' src/styles/global.css | grep -v '^#' | wc -l | awk '{ if ($1 >= 6) exit 0; exit 1 }' && grep -q 'color-scheme: dark' src/styles/global.css</automated>
  </verify>
  <done>GitHub Dark token set exists; dark mode is default; Expressive Code palette will not clash.</done>
</task>

<task type="auto">
  <name>Task 2: Shared .btn and .card primitives</name>
  <files>src/styles/global.css</files>
  <action>
    Add shared component classes per D-97 and D-98:

    `.btn` — inline-flex, align center, padding ~0.6rem 1.1rem, font-weight 600, text-decoration none, border 1px solid color-mix from `--accent`, border-radius `var(--radius)`, min-height 44px for tap target readiness (supports D-101).

    `.btn-primary` — extends `.btn` with accent-tinted background (mirror current hero primary CTA pattern).

    `.card` — surface bg `--surface`, border 1px `--border`, border-radius `var(--radius)`, padding 1.25rem. Flat border-only surface per discretion (no heavy shadow).

    Keep existing `.project-card` rules temporarily but add comment that Plan 03 migrates markup to `.card`. Do not delete `.project-card` in this plan — downstream plans depend on current markup until migrated.

    Add `.tag` or extend `.project-card__tag` pattern using accent highlight per D-86 (subtle accent mix background).
  </action>
  <verify>
    <automated>grep -q '\.btn-primary' src/styles/global.css && grep -q '\.card' src/styles/global.css && grep -q 'var(--radius)' src/styles/global.css</automated>
  </verify>
  <done>.btn, .btn-primary, and .card classes defined with ~4px radius and accent restraint.</done>
</task>

<task type="auto">
  <name>Task 3: Global focus-visible rings</name>
  <files>src/styles/global.css</files>
  <action>
    Per D-104 (foundation for Phase 6 QUAL-02), add `:focus-visible` styles for `a`, `button`, `summary`, and `.btn`. Use 2px outline or box-shadow ring in `--accent` with offset so rings are visible on dark bg. Do not remove default focus for mouse users — use `:focus-visible` only, not bare `:focus` on all elements.

  </action>
  <verify>
    <automated>grep -q ':focus-visible' src/styles/global.css && grep -qE '(outline|box-shadow)' src/styles/global.css</automated>
  </verify>
  <done>All primary interactive elements have visible keyboard focus rings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS tokens → rendered pages | Token values affect sitewide contrast and readability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | src/styles/global.css | accept | Static CSS only; no user input |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
npm run build succeeds after token refresh; dist HTML still renders without missing stylesheet.
</verification>

<success_criteria>
GitHub Dark tokens, .btn/.card primitives, and focus rings exist in global.css and build passes.
</success_criteria>

<output>
Create `.planning/phases/05-design-system-layout-polish/05-01-SUMMARY.md` when done
</output>
