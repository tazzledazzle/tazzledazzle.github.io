---
phase: 05-design-system-layout-polish
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/components/Hero.astro
  - src/components/ProjectCard.astro
  - src/components/ProjectTease.astro
  - src/components/CareerTimeline.astro
  - src/styles/global.css
autonomous: true
requirements: [DESN-01]
must_haves:
  truths:
    - "Hero CTAs use shared .btn classes with no isolated style block"
    - "Project cards use .card surface and unified action buttons"
    - "Homepage hero spans full hiring width with project tease grid below"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Token-driven hero without scoped style block"
      min_lines: 25
    - path: "src/components/ProjectCard.astro"
      provides: "Card using .card and .btn"
      contains: "card"
  key_links:
    - from: "src/components/Hero.astro"
      to: "src/styles/global.css"
      via: ".btn-primary classes"
      pattern: "btn-primary"
    - from: "src/components/ProjectCard.astro"
      to: ".card"
      via: "class list includes card"
      pattern: "card"
---

<objective>
Migrate hiring components to shared design-system classes per D-95–D-100, D-97, D-98, D-99 (markup).

Purpose: Eliminate isolated Hero/Career/Tease scoped styles; unify CTAs and cards.
Output: Hero, ProjectCard, ProjectTease, CareerTimeline using global tokens and primitives.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-design-system-layout-polish/05-CONTEXT.md
@src/components/Hero.astro
@src/components/ProjectCard.astro
@src/components/ProjectTease.astro
@src/components/CareerTimeline.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Hero to global tokens and .btn</name>
  <files>src/components/Hero.astro, src/styles/global.css</files>
  <action>
    Per D-100 and D-97: Remove entire scoped `<style>` block from Hero.astro. Move remaining hero-specific rules to global.css under `.hero` namespace using design tokens (`.hero__greeting` muted, `.hero__exploring` accent italic, `.hero__role` large heading scale per D-90).

    Per D-95: Hero section uses full width within `layout-hiring` container — no artificial max-width on hero root; keep specialty/impact max-width ~60ch optional for readability.

    Replace `hero__cta` / `hero__cta--primary` with `.btn` and `.btn-primary` on anchor elements. Keep CTA pair Resume + GitHub per D-48 (do not add LinkedIn).

    Add `.hero` bottom margin/padding aligned to 2.5rem section gap (D-96).
  </action>
  <verify>
    <automated>! grep -q '<style>' src/components/Hero.astro && grep -q 'btn-primary' src/components/Hero.astro && grep -q '\.hero' src/styles/global.css</automated>
  </verify>
  <done>Hero has no scoped styles; CTAs use .btn-primary; hero layout full hiring width.</done>
</task>

<task type="auto">
  <name>Task 2: Unify ProjectCard and ProjectTease with .card</name>
  <files>src/components/ProjectCard.astro, src/components/ProjectTease.astro, src/styles/global.css</files>
  <action>
    Per D-98 and D-97: Update ProjectCard.astro — add `card` to class list (keep `project-card` BEM elements for structure or consolidate to `.card` + modifiers). Action links use `.btn` for GitHub/demo or `.btn`-styled text links per discretion — at minimum primary demo link uses `.btn`.

    Migrate ProjectTease scoped styles to global.css (`.project-tease`, `__header`, `__grid`). Per D-95: grid below hero with responsive 1-col mobile / 2-col desktop. Section top border + 2.5rem spacing (D-96).

    Deprecate redundant `.project-card` surface rules in global.css if fully superseded by `.card` — keep BEM child selectors for title/tags/actions.

    Header resume link styling deferred to Plan 04 (chrome).
  </action>
  <verify>
    <automated>grep -q 'card' src/components/ProjectCard.astro && ! grep -q '<style>' src/components/ProjectTease.astro && grep -q 'project-tease' src/styles/global.css</automated>
  </verify>
  <done>Project cards and tease grid use shared .card token; tease scoped styles removed.</done>
</task>

<task type="auto">
  <name>Task 3: CareerTimeline markup cleanup</name>
  <files>src/components/CareerTimeline.astro</files>
  <action>
    Per D-99: Remove scoped `<style>` block from CareerTimeline.astro — all styles now live in global.css from Plan 02. Verify markup classes match global selectors (`.career-timeline__list`, `__item`, `__dates`, `__content`). No truncation of bullets (preserve D-54). Education section inherits heading scale.

    Ensure mobile stack: single column below 640px per existing responsive rule in global.css.
  </action>
  <verify>
    <automated>! grep -q '<style>' src/components/CareerTimeline.astro && grep -q 'career-timeline' src/styles/global.css</automated>
  </verify>
  <done>Career timeline uses global accent-rail styles only; no component scoped CSS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component markup → global CSS | Class renames must stay wired to global.css selectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Hero/ProjectCard markup | mitigate | Build + verify:phase3 regression ensures hiring content still renders |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm run build && npm run verify:phase3
</verification>

<success_criteria>
No scoped style blocks in Hero, ProjectTease, CareerTimeline; cards and CTAs use design-system classes.
</success_criteria>

<output>
Create `.planning/phases/05-design-system-layout-polish/05-03-SUMMARY.md` when done
</output>
