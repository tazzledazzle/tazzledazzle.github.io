---
phase: 05-design-system-layout-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/styles/global.css
  - src/layouts/MainLayout.astro
  - src/layouts/PostLayout.astro
autonomous: true
requirements: [DESN-01, DESN-04]
must_haves:
  truths:
    - "Hiring pages use 72rem wide layout; blog posts use ~65ch prose column"
    - "Body copy renders at full --fg; only meta/captions use --muted"
    - "Blog posts show clear h1/h2/h3 hierarchy with comfortable line length"
  artifacts:
    - path: "src/layouts/MainLayout.astro"
      provides: "layout-hiring class on main"
      contains: "layout-hiring"
    - path: "src/layouts/PostLayout.astro"
      provides: "layout-prose class on article or main wrapper"
      contains: "layout-prose"
    - path: "src/styles/global.css"
      provides: "Hand-rolled post-body prose styles"
      contains: ".post-body"
  key_links:
    - from: "src/layouts/MainLayout.astro"
      to: "src/styles/global.css"
      via: "main.layout-hiring max-width 72rem"
      pattern: "layout-hiring"
    - from: "src/layouts/PostLayout.astro"
      to: ".post-body"
      via: "65ch max-width prose rules"
      pattern: "max-width:\\s*65ch"
---

<objective>
Split hiring vs blog layout widths and implement typography + hand-rolled prose per D-90–D-96, D-91, D-92, D-99 (CSS portion), DESN-01, DESN-04.

Purpose: Fix global 72ch main constraint; deliver recruiter-wide hiring layout and reader-narrow blog column.
Output: Layout utility classes, heading scale, `.post-body` prose, career-timeline global styles (accent rail).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-design-system-layout-polish/05-CONTEXT.md
@src/styles/global.css
@src/layouts/MainLayout.astro
@src/layouts/PostLayout.astro
@src/components/CareerTimeline.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hiring vs prose layout width split</name>
  <files>src/styles/global.css, src/layouts/MainLayout.astro, src/layouts/PostLayout.astro</files>
  <action>
    Per D-93 and D-94, replace bare `main { max-width: 72ch }` with:

    `main.layout-hiring` — max-width 72rem, horizontal padding 1.25rem, vertical padding 2.5rem (D-93).

    `main.layout-prose` OR `.post-article.layout-prose` — max-width 65ch centered for blog reading (D-94). Apply via PostLayout wrapping article or main class.

    In MainLayout.astro: add `class="layout-hiring"` to `<main>` by default.

    In PostLayout.astro: override to prose layout — either pass a prop to MainLayout or add `layout-prose` class on article and adjust CSS so post pages are 65ch while hiring pages stay 72rem. Prefer: MainLayout accepts optional `layout?: 'hiring' | 'prose'` prop; PostLayout passes `layout="prose"`.

    Per D-96: add `.section-gap` or use consistent `margin-bottom: 2.5rem` on major sections (`.blog-section`, `.project-tease`, page-level `section` siblings). Document pattern in global.css comment.

    Blog index (`/blog/`) stays on hiring width (72rem) per CONTEXT — only single post body is 65ch.
  </action>
  <verify>
    <automated>grep -q 'layout-hiring' src/layouts/MainLayout.astro && grep -qE 'layout-prose|prose' src/layouts/PostLayout.astro && grep -q '72rem' src/styles/global.css && grep -q '65ch' src/styles/global.css</automated>
  </verify>
  <done>Hiring pages 72rem; post reading column 65ch; 2.5rem section rhythm defined.</done>
</task>

<task type="auto">
  <name>Task 2: Typography scale and body color fix</name>
  <files>src/styles/global.css</files>
  <action>
    Per D-90, D-92: Remove or narrow global `p { color: var(--muted) }` — body paragraphs use `color: var(--fg)`. Scope muted color to `.post-meta`, `.career-timeline__dates`, captions, `.hero__greeting`, and similar meta selectors only.

    Per D-90: Add heading scale — h1 clamp/large (e.g. 2–2.5rem, font-weight 600), h2 1.5rem, h3 1.25rem with increased margin-top on h2/h3 in `.post-body` and hiring pages. Lighter weight on large headings if consistent with GitHub aesthetic.

    Per D-89: Confirm Inter stack unchanged on `html, body`.

    Add `.layout-hiring h1` page title styles for about/work/career pages if needed.
  </action>
  <verify>
    <automated>grep -qE '\.post-body|h1|h2' src/styles/global.css && grep -v '^#' src/styles/global.css | grep -c 'var(--fg)' | awk '{ if ($1 >= 2) exit 0; exit 1 }'</automated>
  </verify>
  <done>Body text full fg; headings use size-scale hierarchy; meta text muted only.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-rolled blog prose and career timeline CSS</name>
  <files>src/styles/global.css</files>
  <action>
    Per D-91 and DESN-04, expand `.post-body` styles (hand-rolled, NOT @tailwindcss/typography):

    - font-size ~1.0625rem, line-height ~1.7
    - max-width 65ch (if not inherited from layout)
    - h2 margin-top 2rem, h3 margin-top 1.5rem
    - `ul`/`ol` spacing, `blockquote` left border using `--border`
    - `a` within prose use `--accent`

    Per D-102 (partial): wrap rules for `.post-body pre`, `.post-body table`, `.expressive-code` parent — `overflow-x: auto; max-width: 100%` so code blocks scroll horizontally inside 65ch column without page-level scroll.

    Per D-99: Add `.career-timeline` global styles (migrate from CareerTimeline.astro scoped block in Plan 03): accent-colored left rail (`border-left: 2px solid var(--accent)` on content column or pseudo-element), date labels muted, responsive single-column stack below 640px. Plan 03 will remove scoped `<style>` from component.
  </action>
  <verify>
    <automated>grep -q '\.post-body' src/styles/global.css && grep -q '1.0625rem' src/styles/global.css && grep -q 'career-timeline' src/styles/global.css && grep -q 'overflow-x' src/styles/global.css</automated>
  </verify>
  <done>Prose readable at 65ch; code/tables scroll contained; career timeline CSS ready in global.css.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layout CSS → all pages | Width changes affect recruiter and reader UX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | Layout class wiring | mitigate | Automated grep verifies layout-hiring and 65ch in CSS and layouts |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm run build && grep -q 'layout-hiring' dist/index.html && npm run test:unit
</verification>

<success_criteria>
Layout split live; typography and prose styles applied; career timeline CSS in global.css.
</success_criteria>

<output>
Create `.planning/phases/05-design-system-layout-polish/05-02-SUMMARY.md` when done
</output>
