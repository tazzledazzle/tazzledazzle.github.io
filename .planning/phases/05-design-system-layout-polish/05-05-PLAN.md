---
phase: 05-design-system-layout-polish
plan: 05
type: execute
wave: 5
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - scripts/verify-phase5-design.mjs
  - package.json
  - .planning/phases/05-design-system-layout-polish/05-VALIDATION.md
autonomous: true
requirements: [DESN-01, DESN-02, DESN-03, DESN-04, DESN-05]
must_haves:
  truths:
    - "Automated gate verifies DESN-01 through DESN-05 markers in dist/"
    - "npm run verify:phase5 passes on complete Phase 5 implementation"
    - "Validation matrix documents manual 375px checks"
  artifacts:
    - path: "scripts/verify-phase5-design.mjs"
      provides: "Post-build design system verification CLI"
    - path: ".planning/phases/05-design-system-layout-polish/05-VALIDATION.md"
      provides: "DESN requirement smoke matrix"
  key_links:
    - from: "package.json"
      to: "scripts/verify-phase5-design.mjs"
      via: "npm run verify:phase5"
      pattern: "verify:phase5"
---

<objective>
Add Phase 5 verification gate and validation matrix covering DESN-01 through DESN-05 and locked decisions D-85–D-107.

Purpose: Close phase with measurable design acceptance criteria matching Phase 3/4 verify patterns.
Output: verify-phase5-design.mjs, npm script, 05-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-design-system-layout-polish/05-CONTEXT.md
@scripts/verify-phase3-hiring.mjs
@scripts/verify-phase4-blog.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify-phase5-design.mjs smoke gate</name>
  <files>scripts/verify-phase5-design.mjs, package.json</files>
  <action>
    Create scripts/verify-phase5-design.mjs following verify-phase3/4 patterns (read dist HTML/CSS, exit 1 on failure).

    DESN-01 / D-85–D-89, D-97–D-98: dist/_astro/*.css or inlined CSS markers — grep built CSS for `--border` or `--surface`, `.btn-primary`, `.card` class definitions.

    DESN-03 / D-85: dist/index.html or CSS contains `color-scheme: dark` or dark bg token.

    DESN-04 / D-91, D-94: Sample blog post dist HTML — `.post-body` or layout-prose present; CSS contains `65ch`.

    DESN-05 / D-105–D-106: dist/index.html contains nav links to `/about/`, `/work/`, `/career/`, `/blog/`; contains text "Work" (not required to say "Projects"); Header or nav region present.

    DESN-02 / D-101–D-103: dist CSS contains `position: sticky` on site-header; mobile-nav class present in dist HTML; `:focus-visible` rule in CSS.

    D-100: Verify src/components/Hero.astro has no `<style>` tag (read source file, not dist).

    D-104: CSS contains `:focus-visible`.

    D-93: CSS contains `72rem` for hiring layout.

    Regression: run npm run verify:phase3 and verify:phase4 as part of verify:phase5 OR document as chained script: `npm run build && node scripts/verify-phase5-design.mjs` with phase5 script calling phase3/4 checks optionally.

    Add package.json script: `"verify:phase5": "npm run build && node scripts/verify-phase5-design.mjs"`. Keep verify:phase3 and verify:phase4 intact.
  </action>
  <verify>
    <automated>npm run verify:phase5</automated>
  </verify>
  <done>verify:phase5 passes when all Phase 5 plans complete.</done>
</task>

<task type="auto">
  <name>Task 2: Create 05-VALIDATION.md matrix</name>
  <files>.planning/phases/05-design-system-layout-polish/05-VALIDATION.md, scripts/verify-phase5-design.mjs</files>
  <action>
    Create 05-VALIDATION.md with rows for DESN-01 through DESN-05:

    Each row: requirement text, decision IDs (D-85–D-107 as applicable), automated check (verify script assertion), manual check column.

    Manual checks (document, not automate):
    - DESN-02: 375px viewport — no page-level horizontal scroll on home, /work/, /career/, /about/, sample blog post; code blocks may scroll internally (D-102).
    - DESN-01: Visual cohesion between site chrome and code blocks (GitHub Dark alignment D-85/D-71).
    - DESN-04: Heading hierarchy readability on long post.

    Map every D-85 through D-107 to a row or sub-bullet. Confirm D-106 explicitly: nav shows "Work" label.

    Sync verify-phase5-design.mjs assertions with automated column. Every DESN requirement must have automated OR documented manual gate.

    Run npm run verify:phase5 until green after Plans 01–04 are complete (this plan runs last).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-design-system-layout-polish/05-VALIDATION.md && grep -c 'DESN-0' .planning/phases/05-design-system-layout-polish/05-VALIDATION.md | awk '{ if ($1 >= 5) exit 0; exit 1 }'</automated>
  </verify>
  <done>Validation matrix covers all DESN requirements and D-85–D-107 decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verify script → dist/ | Reads built artifacts only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | verify script assertions | mitigate | Sync with 05-VALIDATION.md matrix |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm run verify:phase5 && npm run verify:phase3 && npm run verify:phase4
</verification>

<success_criteria>
All DESN requirements gated; verify:phase5 green; prior phase regressions pass.
</success_criteria>

<output>
Create `.planning/phases/05-design-system-layout-polish/05-05-SUMMARY.md` when done
</output>
