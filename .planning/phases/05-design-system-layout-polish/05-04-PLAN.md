---
phase: 05-design-system-layout-polish
plan: 04
type: execute
wave: 4
depends_on: [05-01, 05-02]
files_modified:
  - src/components/Header.astro
  - src/components/MobileNav.astro
  - src/components/Footer.astro
  - src/lib/nav.ts
  - src/styles/global.css
autonomous: true
requirements: [DESN-02, DESN-05]
must_haves:
  truths:
    - "Header stays sticky on scroll with resume always accessible"
    - "Mobile menu has full-width panel and 44px tap targets at 375px"
    - "Active nav link shows underline or bold for current page"
    - "Nav label Work links to /work/ per D-05 and D-106"
    - "Footer shows copyright and social with subtle top border"
  artifacts:
    - path: "src/lib/nav.ts"
      provides: "isActiveNavPath helper"
      exports: ["isActiveNavPath"]
    - path: "src/components/Header.astro"
      provides: "Sticky header with active nav states"
      contains: "site-header--sticky"
    - path: "src/components/MobileNav.astro"
      provides: "Polished hamburger panel"
      contains: "mobile-nav"
  key_links:
    - from: "src/components/Header.astro"
      to: "src/lib/nav.ts"
      via: "isActiveNavPath(Astro.url.pathname, item.data.url)"
      pattern: "isActiveNavPath"
    - from: "src/components/Header.astro"
      to: "src/data/navigation.yml"
      via: "Work label unchanged"
      pattern: "Work"
---

<objective>
Polish site chrome: sticky header, active nav, mobile hamburger, footer, and overflow containment per D-101–D-107, DESN-02, DESN-05.

Purpose: Deliver mobile-usable navigation and consistent chrome across all pages.
Output: Sticky header, active nav styling, polished MobileNav, minimal footer, nav helper.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-design-system-layout-polish/05-CONTEXT.md
@src/components/Header.astro
@src/components/MobileNav.astro
@src/components/Footer.astro
@src/data/navigation.yml
@src/styles/global.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nav active-path helper</name>
  <files>src/lib/nav.ts, src/lib/nav.test.ts</files>
  <behavior>
    - Test 1: isActiveNavPath('/work/', '/work/') returns true
    - Test 2: isActiveNavPath('/work/foo/', '/work/') returns true (prefix match for section)
    - Test 3: isActiveNavPath('/about/', '/work/') returns false
    - Test 4: isActiveNavPath('/', '/blog/') returns false; isActiveNavPath('/blog/', '/blog/') returns true
    - Test 5: isActiveNavPath('/blog/tags/foo/', '/blog/') returns true for blog section
  </behavior>
  <action>
    Create src/lib/nav.ts exporting `isActiveNavPath(currentPath: string, navUrl: string): boolean` per D-105. Normalize trailing slashes. Home `/` only active for exact match. Section paths active when current path equals nav URL or starts with nav URL (e.g. blog tags under /blog/).

    Create src/lib/nav.test.ts using node:test. Wire into existing `npm run test:unit` glob if needed.

    Per D-106: Do NOT rename "Work" to "Projects" — navigation.yml title stays "Work" at /work/.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <done>isActiveNavPath tested and exported; Work label preserved in data.</done>
</task>

<task type="auto">
  <name>Task 2: Sticky header, active nav, resume .btn</name>
  <files>src/components/Header.astro, src/components/MobileNav.astro, src/styles/global.css</files>
  <action>
    Per D-103: Make `.site-header` sticky — `position: sticky; top: 0; z-index: 50; background: var(--bg)` (CSS only per discretion). Ensure mobile nav panel stacks above content when open.

    Per D-105: In Header.astro and MobileNav.astro, import isActiveNavPath; pass `Astro.url.pathname` from Header to MobileNav as prop. Add `aria-current="page"` and class `is-active` on matching nav links. Style `.site-header__nav a.is-active` with font-weight 700 and/or `text-decoration: underline` (underline-offset 4px).

    Apply `.btn .btn-primary` to header "Download Resume" link per D-97.

    Per D-101: Polish `.mobile-nav` — full-width panel below header inner, min-height 44px tap targets on links, increased padding/gap, summary button min 44px. Keep `<details>`/`<summary>` pattern (D-38/D-59 — no drawer replacement).

    Per D-102: Confirm global.css has `overflow-x: auto` on `.post-body pre`, `table`, `.expressive-code` (from Plan 02); add `overflow-x: hidden` on `body` only if needed after testing — prefer NOT hiding body overflow if code wrappers suffice. Add `max-width: 100%` on `img` in prose to prevent horizontal page scroll at 375px.

    DESN-05: Verify all four nav entries (About, Work, Career, Blog) render from navigation.yml with working hrefs — no markup changes to URLs.
  </action>
  <verify>
    <automated>grep -q 'sticky' src/styles/global.css && grep -q 'isActiveNavPath' src/components/Header.astro && grep -q 'is-active' src/styles/global.css</automated>
  </verify>
  <done>Sticky header, active states, polished mobile nav, resume uses .btn-primary.</done>
</task>

<task type="auto">
  <name>Task 3: Footer minimal polish</name>
  <files>src/components/Footer.astro, src/styles/global.css</files>
  <action>
    Per D-107: Polish footer spacing only — subtle top border (existing), consistent padding with header (1.5rem vertical), muted copyright text, social links with focus rings inherited. No sitemap links (deferred). Ensure GitHub, LinkedIn, email remain visible per HIRE-07.

    Align footer copy color to `--muted`; keep structure minimal.
  </action>
  <verify>
    <automated>grep -q 'site-footer' src/styles/global.css && test -f src/components/Footer.astro</automated>
  </verify>
  <done>Footer polished with minimal copyright + social layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nav path matching | Incorrect active state misleads orientation — low security risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | isActiveNavPath | mitigate | Unit tests for path edge cases |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm run build && npm run test:unit && npm run verify:phase3
</verification>

<success_criteria>
Sticky header, active nav, mobile polish, and footer complete; DESN-02 and DESN-05 satisfied in chrome.
</success_criteria>

<output>
Create `.planning/phases/05-design-system-layout-polish/05-04-SUMMARY.md` when done
</output>
