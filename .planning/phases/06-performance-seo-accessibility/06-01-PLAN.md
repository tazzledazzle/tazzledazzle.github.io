---
phase: 06-performance-seo-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Seo.astro
  - src/layouts/MainLayout.astro
  - src/layouts/PostLayout.astro
  - src/lib/seo/description.ts
  - src/lib/seo/description.test.ts
  - src/pages/[...slug].astro
  - public/og-default.png
  - public/fonts/inter-latin-400.woff2
  - public/fonts/inter-latin-600.woff2
  - src/styles/global.css
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/work.astro
  - src/pages/career.astro
  - src/pages/blog/index.astro
  - src/pages/blog/archive/index.astro
  - src/pages/blog/tags/[tag].astro
  - scripts/generate-og-default.mjs
  - package.json
autonomous: true
requirements: [QUAL-03, QUAL-01]
must_haves:
  truths:
    - "Every key hiring page has unique meta description, OG, and Twitter Card tags"
    - "Archived blog posts emit noindex only without OG/Twitter enrichment"
    - "Inter font loads from self-hosted woff2 with preload and font-display swap"
    - "public/og-default.png exists and is referenced as default OG image"
  artifacts:
    - path: "src/components/Seo.astro"
      provides: "Reusable OG + Twitter head tags"
    - path: "public/og-default.png"
      provides: "Default social share image"
    - path: "public/fonts/inter-latin-400.woff2"
      provides: "Self-hosted Inter regular latin subset"
    - path: "public/fonts/inter-latin-600.woff2"
      provides: "Self-hosted Inter semibold latin subset"
  key_links:
    - from: "src/layouts/MainLayout.astro"
      to: "src/components/Seo.astro"
      via: "import and render in head"
      pattern: "Seo"
    - from: "src/layouts/PostLayout.astro"
      to: "MainLayout"
      via: "skip Seo enrichment when isArchived"
      pattern: "isArchived"
---

<objective>
Ship SEO head foundation, self-hosted Inter, default OG image, and per-page meta descriptions per D-108–D-114, D-128, D-129, and D-110.

Purpose: QUAL-03 meta/OG tags and QUAL-01 font performance prerequisites for Lighthouse gates.
Output: Seo.astro, updated layouts/pages, og-default.png, self-hosted fonts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-performance-seo-accessibility/06-CONTEXT.md
@src/layouts/MainLayout.astro
@src/layouts/PostLayout.astro
@src/pages/blog/index.astro
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Seo.astro and extend MainLayout head</name>
  <files>src/components/Seo.astro, src/layouts/MainLayout.astro, src/layouts/PostLayout.astro</files>
  <action>
    Create src/components/Seo.astro accepting props: title, description (optional), ogImage (optional, default /og-default.png), canonicalUrl (optional), skipSocial (boolean, default false), jsonLd (optional Record or array for later Plan 02).

  When skipSocial is false and description is present, emit full Open Graph tags per D-114: og:title, og:description, og:type (website or article passed in), og:url (canonical or Astro.url), og:image (absolute URL using Astro.site + ogImage), og:site_name. Emit Twitter Card tags: twitter:card summary_large_image, twitter:title, twitter:description, twitter:image.

  Extend MainLayout Props: add optional jsonLd prop (D-118 shell), optional ogType default website, optional skipSocial default false. Import Seo.astro in head after title/meta description/robots. Pass title, description, jsonLd, skipSocial.

  Update PostLayout per D-115: when isArchived, pass skipSocial=true to MainLayout and omit description prop (only title + robots noindex from existing pattern). Non-archived posts pass description and skipSocial=false.

  Do NOT add JSON-LD builders yet — only render jsonLd prop as script type application/ld+json when provided.
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');const checks=[['og:title','OG title'],['twitter:card','Twitter card'],['og:image','OG image']];for(const [n,l] of checks){if(!h.includes(n)) {console.error('missing',l);process.exit(1)}} console.log('seo head ok')"</automated>
  </verify>
  <done>Built home page HTML contains og:title, twitter:card, and og:image meta tags.</done>
</task>

<task type="auto">
  <name>Task 2: Self-host Inter latin 400+600 with preload</name>
  <files>public/fonts/inter-latin-400.woff2, public/fonts/inter-latin-600.woff2, src/styles/global.css, src/layouts/MainLayout.astro</files>
  <action>
    Per D-110 and D-129: obtain Inter latin subset woff2 files for weights 400 and 600 only (use @fontsource/inter files subsetted to latin, or download from fontsource release). Place at public/fonts/inter-latin-400.woff2 and public/fonts/inter-latin-600.woff2.

    Add @font-face rules in src/styles/global.css for both weights with font-display: swap and font-family Inter. Update body font-family stack to prefer Inter.

    In MainLayout head, add link rel=preload as=font type=font/woff2 crossorigin for the 400 weight file (primary text). Remove any external Google Fonts CDN links if present.

    Do not load weights beyond 400 and 600.
  </action>
  <verify>
    <automated>npm run build && test -f public/fonts/inter-latin-400.woff2 && test -f public/fonts/inter-latin-600.woff2 && node -e "const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');if(!h.includes('preload')||!h.includes('woff2')){process.exit(1)};const css=fs.readdirSync('dist/_astro').find(f=>f.endsWith('.css'));if(!css||!fs.readFileSync('dist/_astro/'+css,'utf8').includes('font-display'))process.exit(1);console.log('fonts ok')"</automated>
  </verify>
  <done>Font files exist, CSS uses font-display swap, and built HTML preloads woff2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: og-default.png, page descriptions, and post description derivation</name>
  <files>public/og-default.png, scripts/generate-og-default.mjs, package.json, src/lib/seo/description.ts, src/lib/seo/description.test.ts, src/pages/[...slug].astro, src/pages/index.astro, src/pages/about.astro, src/pages/work.astro, src/pages/career.astro, src/pages/blog/index.astro, src/pages/blog/archive/index.astro, src/pages/blog/tags/[tag].astro</files>
  <behavior>
    - deriveDescription(postBody, frontmatterDescription): returns frontmatterDescription when present; otherwise first non-heading paragraph text trimmed to 155 chars with ellipsis
    - deriveDescription returns empty string for whitespace-only body
  </behavior>
  <action>
    Per D-128: add devDependency sharp (pinned). Create scripts/generate-og-default.mjs rendering 1200x630 dark branded PNG with name and role via sharp SVG input; run script to write public/og-default.png and commit output.

    Per D-112: create src/lib/seo/description.ts with deriveDescription(body, explicit?) and tests in description.test.ts. In [...slug].astro when NOT archived, pass deriveDescription(post.body, post.data.description) to PostLayout (D-115 archived unchanged).

    Add explicit MainLayout description props:
    - index, about, work, career, blog/index — unique intentional copy (refine existing where present)
    - blog/archive/index.astro — year archive browse description
    - blog/tags/[tag].astro — dynamic tag-specific description

    Descriptions 120–160 chars, unique per page. Ensures QUAL-03 on all 6 Lighthouse pages including kotlin-cheatsheet post.
  </action>
  <verify>
    <automated>node --test src/lib/seo/description.test.ts && npm run build && node -e "const fs=require('fs');const h=fs.readFileSync('dist/2024/11/08/kotlin-cheatsheet/index.html','utf8');if(!h.includes('og:description')||!h.includes('meta name=\"description\"'))process.exit(1);const a=fs.readFileSync('dist/blog/archive/index.html','utf8');if(!a.includes('meta name=\"description\"'))process.exit(1);if(!fs.existsSync('public/og-default.png'))process.exit(1);console.log('descriptions ok')"</automated>
  </verify>
  <done>og-default.png committed; kotlin-cheatsheet and archive dist HTML have meta description and og:description.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built HTML head | Meta/OG tags are public; must not leak secrets |
| Font files | Static assets served from GitHub Pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | scripts/generate-og-default.mjs | mitigate | Script only writes public/og-default.png; no network fetch of untrusted assets |
| T-06-SC | Tampering | npm font packages if used | mitigate | Use @fontsource/inter from npm registry only; verify package name before install |
</threat_model>

<verification>
npm run build succeeds. dist/index.html and dist/blog/index.html contain OG/Twitter tags and meta descriptions. Archived post dist HTML must NOT contain og:description when verified in Plan 05.
</verification>

<success_criteria>
Seo component wired; self-hosted Inter with preload; og-default.png committed; all key pages have unique descriptions; archived posts skip social meta per D-115.
</success_criteria>

<output>
Create `.planning/phases/06-performance-seo-accessibility/06-01-SUMMARY.md` when done
</output>
