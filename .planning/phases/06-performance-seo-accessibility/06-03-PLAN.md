---
phase: 06-performance-seo-accessibility
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - scripts/audit-contrast.mjs
  - src/styles/global.css
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "Design token color pairs meet WCAG 2.1 AA contrast minimums"
    - "Contrast audit script fails CI when tokens regress"
  artifacts:
    - path: "scripts/audit-contrast.mjs"
      provides: "WCAG contrast ratio gate for CSS tokens"
    - path: "src/styles/global.css"
      provides: "Adjusted token values passing audit"
  key_links:
    - from: "scripts/audit-contrast.mjs"
      to: "src/styles/global.css"
      via: "parse :root custom properties --fg --bg --muted --accent --surface"
      pattern: "--fg|--bg|--muted"
---

<objective>
Run contrast audit on GitHub Dark tokens and fix WCAG 2.1 AA failures per D-124.

Purpose: QUAL-02 baseline contrast independent of Phase 5 token assumptions.
Output: audit-contrast.mjs and corrected global.css token values.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-performance-seo-accessibility/06-CONTEXT.md
@.planning/phases/05-design-system-layout-polish/05-CONTEXT.md
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contrast audit script</name>
  <files>scripts/audit-contrast.mjs</files>
  <action>
    Create scripts/audit-contrast.mjs that reads src/styles/global.css :root block and extracts hex values for --fg, --bg, --muted, --accent, --surface, --border.

    Implement relative luminance and contrast ratio per WCAG 2.1. Test pairs that appear in UI:
    - --fg on --bg (body text) — require ≥ 4.5:1
    - --muted on --bg (secondary text) — require ≥ 4.5:1
    - --fg on --surface (cards) — require ≥ 4.5:1
    - --accent on --bg (link text) — require ≥ 4.5:1
    - --accent on --surface (links in cards if used) — require ≥ 4.5:1

    Print failing pairs with actual ratios. Exit 1 on any failure, 0 when all pass.

    No external dependencies — pure Node.
  </action>
  <verify>
    <automated>node scripts/audit-contrast.mjs; echo exit:$?</automated>
  </verify>
  <done>Audit script runs and reports pass or fail with contrast ratios.</done>
</task>

<task type="auto">
  <name>Task 2: Fix failing token contrast values</name>
  <files>src/styles/global.css</files>
  <action>
    Run node scripts/audit-contrast.mjs. For each failing pair per D-124, adjust :root token hex values minimally while preserving GitHub Dark aesthetic (lighten --fg/--muted or darken --bg/--surface, tune --accent if link contrast fails).

    Re-run audit until exit 0. Avoid changing unrelated layout rules — tokens only.

    Document adjusted values in code comments only if non-obvious (e.g., muted bumped for AA).
  </action>
  <verify>
    <automated>node scripts/audit-contrast.mjs</automated>
  </verify>
  <done>audit-contrast.mjs exits 0 — all token pairs meet WCAG 2.1 AA 4.5:1.</done>
</task>

<task type="auto">
  <name>Task 3: Wire contrast audit into local verify prep</name>
  <files>scripts/audit-contrast.mjs, package.json</files>
  <action>
    Add package.json script: "audit:contrast": "node scripts/audit-contrast.mjs".

    Keyboard nav validation per D-125 is delegated to axe Playwright in Plan 06-04 — no skip-link added.

    Confirm Phase 5 :focus-visible rules remain untouched in global.css.
  </action>
  <verify>
    <automated>npm run audit:contrast</automated>
  </verify>
  <done>npm run audit:contrast passes; focus-visible styles preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-contrast.mjs | Reads local CSS only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | global.css tokens | mitigate | Automated contrast gate prevents silent regressions |
</threat_model>

<verification>
npm run audit:contrast exits 0. Visual dark theme still coherent after token tweaks.
</verification>

<success_criteria>
D-124 contrast audit passes for all checked token pairs; D-125 keyboard validation deferred to axe in Plan 04.
</success_criteria>

<output>
Create `.planning/phases/06-performance-seo-accessibility/06-03-SUMMARY.md` when done
</output>
