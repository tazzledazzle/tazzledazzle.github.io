---
phase: 06-performance-seo-accessibility
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/lib/seo/jsonLd.ts
  - src/lib/seo/jsonLd.test.ts
  - src/layouts/PostLayout.astro
  - src/pages/[...slug].astro
  - src/pages/about.astro
autonomous: true
requirements: [QUAL-04]
must_haves:
  truths:
    - "Non-archived blog posts expose rich Article JSON-LD in head"
    - "About page exposes Person JSON-LD with sameAs social links"
    - "Archived posts have no JSON-LD script blocks"
  artifacts:
    - path: "src/lib/seo/jsonLd.ts"
      provides: "Article and Person schema builders"
      exports: ["buildArticleSchema", "buildPersonSchema"]
    - path: "src/pages/about.astro"
      provides: "Person schema injection via jsonLd prop"
  key_links:
    - from: "src/pages/[...slug].astro"
      to: "buildArticleSchema"
      via: "jsonLd prop to PostLayout when not archived"
      pattern: "buildArticleSchema"
    - from: "src/pages/about.astro"
      to: "src/data/social.yml"
      via: "getEntry social + buildPersonSchema"
      pattern: "buildPersonSchema"
---

<objective>
Implement JSON-LD structured data for blog posts and About page per D-116 through D-119 and QUAL-04.

Purpose: Search engines receive Article and Person schema for technical credibility.
Output: jsonLd.ts builders, wired PostLayout and about page.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-performance-seo-accessibility/06-CONTEXT.md
@src/layouts/MainLayout.astro
@src/pages/[...slug].astro
@src/pages/about.astro
@src/data/social.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Article and Person JSON-LD builders</name>
  <files>src/lib/seo/jsonLd.ts, src/lib/seo/jsonLd.test.ts</files>
  <behavior>
    - buildArticleSchema: given title, description, pubDate ISO, author name, canonical url, wordCount number, tags array, image absolute url — returns @type Article with headline, datePublished, author @type Person, url, description, wordCount, keywords comma-joined, image
    - buildPersonSchema: given name, email, sameAs url array — returns @type Person with name, email, sameAs
    - buildArticleSchema omits undefined optional fields
  </behavior>
  <action>
    Create src/lib/seo/jsonLd.ts with buildArticleSchema and buildPersonSchema per D-116 and D-117. Use schema.org context https://schema.org. Author in Article is nested Person with name only.

    Add src/lib/seo/jsonLd.test.ts using node:test (match src/lib/*.test.ts pattern). Export pure functions with no Astro imports.

    Helper: count words from post body string for wordCount (split whitespace, filter empty).
  </action>
  <verify>
    <automated>node --test src/lib/seo/jsonLd.test.ts</automated>
  </verify>
  <done>Unit tests pass for Article and Person schema shape and required fields.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Article JSON-LD on blog posts</name>
  <files>src/pages/[...slug].astro, src/layouts/PostLayout.astro</files>
  <action>
    Per D-116, D-118, D-119: in src/pages/[...slug].astro, when isArchived is false, build Article schema via buildArticleSchema using post title, description, pubDate, author Terence Schumacher from social entry, canonical URL Astro.site + permalink, wordCount from post body, keywords from tags, image Astro.site + /og-default.png.

    Pass jsonLd prop through PostLayout to MainLayout (extend PostLayout Props if needed).

    When isArchived is true, do NOT pass jsonLd (D-119). Confirm dist archived HTML has no application/ld+json script.

    Set Seo ogType to article for non-archived posts via MainLayout/PostLayout prop chain.
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const h=fs.readFileSync('dist/2024/11/08/kotlin-cheatsheet/index.html','utf8');if(!h.includes('application/ld+json')||!h.includes('Article')){process.exit(1)};const arch=fs.readdirSync('dist/2015',{recursive:true}).find(f=>f.endsWith('index.html'));if(arch){const ah=fs.readFileSync('dist/2015/'+arch,'utf8');if(ah.includes('application/ld+json'))process.exit(1)}console.log('article ld ok')"</automated>
  </verify>
  <done>Sample featured post dist HTML contains Article JSON-LD; archived post dist HTML has none.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Person JSON-LD on About page</name>
  <files>src/pages/about.astro</files>
  <action>
    Per D-117: load social entry via getEntry, call buildPersonSchema with name, email, sameAs array of github.url, linkedin.url, twitter.url from social.yml.

    Pass resulting object as jsonLd prop to MainLayout.

    Ensure email appears in schema (public contact email already on page).
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const h=fs.readFileSync('dist/about/index.html','utf8');if(!h.includes('application/ld+json')||!h.includes('Person')||!h.includes('sameAs')){process.exit(1)}console.log('person ld ok')"</automated>
  </verify>
  <done>About page dist HTML contains Person JSON-LD with sameAs social URLs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON-LD in HTML | Public structured data only; no private fields beyond already-public email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | buildPersonSchema | accept | Email and social URLs already rendered on About page |
</threat_model>

<verification>
npm run test:unit passes. Built kotlin-cheatsheet post has Article schema. Built about has Person schema. Archived post has no JSON-LD.
</verification>

<success_criteria>
QUAL-04 satisfied: Article on posts, Person on About, archived excluded per D-119.
</success_criteria>

<output>
Create `.planning/phases/06-performance-seo-accessibility/06-02-SUMMARY.md` when done
</output>
