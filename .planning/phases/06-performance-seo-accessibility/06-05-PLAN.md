---
phase: 06-performance-seo-accessibility
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - docs/IMAGE-POLICY.md
  - scripts/verify-phase6-quality.mjs
  - .planning/phases/06-performance-seo-accessibility/06-VALIDATION.md
  - package.json
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "npm run verify:phase6 passes as Phase 6 regression gate"
    - "06-VALIDATION.md maps every QUAL requirement and D-108 through D-129"
    - "Image policy documents Astro Image requirements for future content"
  artifacts:
    - path: "scripts/verify-phase6-quality.mjs"
      provides: "Post-build QUAL verification CLI"
    - path: ".planning/phases/06-performance-seo-accessibility/06-VALIDATION.md"
      provides: "QUAL requirement smoke matrix"
    - path: "docs/IMAGE-POLICY.md"
      provides: "Astro Image policy for QUAL-06"
  key_links:
    - from: "package.json"
      to: "scripts/verify-phase6-quality.mjs"
      via: "npm run verify:phase6"
      pattern: "verify:phase6"
---

<objective>
Close Phase 6 with image optimization policy, verify-phase6 gate, and validation matrix covering QUAL-01 through QUAL-06 and D-108 through D-129.

Purpose: Measurable acceptance criteria matching Phase 3–5 verify patterns; document QUAL-06 for future images.
Output: IMAGE-POLICY.md, verify-phase6-quality.mjs, 06-VALIDATION.md, npm script.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-performance-seo-accessibility/06-CONTEXT.md
@scripts/verify-phase5-design.mjs
@scripts/verify-phase4-blog.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Astro Image policy</name>
  <files>docs/IMAGE-POLICY.md</files>
  <action>
    Per D-127 and QUAL-06: create docs/IMAGE-POLICY.md stating all future content images must use astro:assets Image component (or @astrojs/image integration if added) with explicit width and height, loading=lazy default, modern formats (webp/avif via Astro optimization), and meaningful alt text.

    Note current blog has zero markdown images — policy is forward-looking. Include minimal code example importing Image from astro:assets.

    Reference check-dist-alt.mjs as enforcement for alt in built HTML.
  </action>
  <verify>
    <automated>test -f docs/IMAGE-POLICY.md && grep -v '^#' docs/IMAGE-POLICY.md | grep -q 'astro:assets'</automated>
  </verify>
  <done>IMAGE-POLICY.md exists with Astro Image requirements per D-127.</done>
</task>

<task type="auto">
  <name>Task 2: Create verify-phase6-quality.mjs</name>
  <files>scripts/verify-phase6-quality.mjs, package.json</files>
  <action>
    Create scripts/verify-phase6-quality.mjs following verify-phase5 pattern:

    Chain regressions: run verify-phase3, verify-phase4, verify-phase5 checks (execSync, fail on error).

    QUAL-03 / D-112–D-114: dist/index.html has meta description, og:title, og:description, twitter:card, og:image pointing to og-default.

    QUAL-04 / D-116–D-119: dist/2024/11/08/kotlin-cheatsheet/index.html has Article JSON-LD; dist/about/index.html has Person JSON-LD; find one archived post HTML — assert NO og:description and NO application/ld+json (D-115, D-119).

    QUAL-01 / D-110, D-129: dist/index.html preloads woff2; public/fonts/inter-latin-400.woff2 exists.

    QUAL-06 / D-128: public/og-default.png exists.

    QUAL-02 / D-124: run npm run audit:contrast.

    QUAL-05: assert lychee.toml, lighthouserc.cjs, tests/a11y/key-pages.spec.ts exist (CI wiring smoke — not full CI run locally).

    D-108 sample post path: /2024/11/08/kotlin-cheatsheet/

    Add package.json: "verify:phase6": "npm run build && node scripts/verify-phase6-quality.mjs"
  </action>
  <verify>
    <automated>npm run verify:phase6</automated>
  </verify>
  <done>verify:phase6 passes when Plans 01–04 complete.</done>
</task>

<task type="auto">
  <name>Task 3: Create 06-VALIDATION.md matrix</name>
  <files>.planning/phases/06-performance-seo-accessibility/06-VALIDATION.md</files>
  <action>
    Create 06-VALIDATION.md with rows for QUAL-01 through QUAL-06. Each row: requirement text, decision IDs (D-108–D-129 mapped), automated check (verify script or CI), manual check column.

    Manual checks (document only):
    - QUAL-01: Confirm Lighthouse mobile scores in CI logs on merge to main
    - QUAL-03: Share preview of home page in Slack/iMessage to confirm OG image renders (optional)

    Map every D-108 through D-129 to a row or sub-bullet. Explicitly note D-111: only Performance+SEO are Lighthouse hard gates; axe handles a11y.

    D-115 archived policy, D-122 main push trigger, D-127 forward-looking image policy.

    Sync automated column with verify-phase6-quality.mjs and CI steps from Plan 04.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-performance-seo-accessibility/06-VALIDATION.md && grep -v '^#' .planning/phases/06-performance-seo-accessibility/06-VALIDATION.md | grep -c 'QUAL-0' | awk '{ if ($1 >= 6) exit 0; exit 1 }'</automated>
  </verify>
  <done>Validation matrix covers all QUAL requirements and D-108–D-129 decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verify script → dist/ | Reads built artifacts only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | verify-phase6-quality.mjs | mitigate | Script is version-controlled; runs in CI via build chain |
</threat_model>

<verification>
npm run verify:phase6 passes. 06-VALIDATION.md has 6 QUAL rows minimum.
</verification>

<success_criteria>
Phase 6 verification gate green; all QUAL requirements traceable to automated or documented manual checks.
</success_criteria>

<output>
Create `.planning/phases/06-performance-seo-accessibility/06-05-SUMMARY.md` when done
</output>
